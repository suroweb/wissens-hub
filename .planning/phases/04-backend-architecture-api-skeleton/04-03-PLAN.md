---
phase: 04-backend-architecture-api-skeleton
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs
  - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadHandler.cs
  - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadValidator.cs
  - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleCommand.cs
  - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleHandler.cs
  - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleValidator.cs
  - api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteCommand.cs
  - api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteHandler.cs
  - api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteValidator.cs
  - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs
  - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleHandler.cs
  - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleValidator.cs
  - api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusQuery.cs
  - api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusHandler.cs
  - api/src/WissensHub.Application/Queries/GetArticleStatus/ArticleStatusDto.cs
  - api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesQuery.cs
  - api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesHandler.cs
  - api/src/WissensHub.Application/Queries/GetUnreadArticles/UnreadArticleDto.cs
  - api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsQuery.cs
  - api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsHandler.cs
  - api/src/WissensHub.Application/Queries/GetReadStats/ReadStatsDto.cs
  - api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesQuery.cs
  - api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesHandler.cs
  - api/src/WissensHub.Application/Queries/GetFavorites/FavoriteDto.cs
  - api/src/WissensHub.Application/Queries/GetDashboardStats/GetDashboardStatsQuery.cs
  - api/src/WissensHub.Application/Queries/GetDashboardStats/GetDashboardStatsHandler.cs
  - api/src/WissensHub.Application/Queries/GetDashboardStats/DashboardStatsDto.cs
  - api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsQuery.cs
  - api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsHandler.cs
  - api/src/WissensHub.Application/Queries/GetAdminReports/AdminReportDto.cs
autonomous: true
requirements: [BACK-01, BACK-02, API-01, API-02, API-03, API-04, API-05, API-06, API-07, API-08, API-09, API-10]

must_haves:
  truths:
    - "Every API endpoint has a matching MediatR command or query record implementing IRequest<ApiResponse<TDto>>"
    - "All 4 POST command handlers return static mock data in ApiResponse envelope"
    - "All 6 GET query handlers return static mock data in ApiResponse envelope"
    - "FluentValidation validators exist for all 4 commands with meaningful rules"
    - "Role-gated operations (GetReadStats, ApproveArticle, GetAdminReports) have RequireGroup attribute"
    - "All mock data uses German content matching Phase 2 sample data (IT-Sicherheit, Datenschutz, etc.)"
  artifacts:
    - path: "api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs"
      provides: "Command record with PageId"
      contains: "IRequest<ApiResponse"
    - path: "api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs"
      provides: "Command with RequireGroup attribute"
      contains: "RequireGroup"
    - path: "api/src/WissensHub.Application/Queries/GetDashboardStats/DashboardStatsDto.cs"
      provides: "DTO with UnreadCount, FavoritesCount, PendingReviewsCount"
      contains: "record DashboardStatsDto"
  key_links:
    - from: "MarkAsReadHandler.cs"
      to: "ApiResponse.cs"
      via: "return type"
      pattern: "ApiResponse<ReadConfirmationDto>.Ok"
    - from: "ApproveArticleCommand.cs"
      to: "RequireGroupAttribute.cs"
      via: "class attribute"
      pattern: "\\[RequireGroup"
    - from: "FlagArticleValidator.cs"
      to: "FlagArticleCommand.cs"
      via: "AbstractValidator<T>"
      pattern: "AbstractValidator<FlagArticleCommand>"
---

<objective>
Create all MediatR commands, queries, handlers, validators, and DTOs for the 10 API endpoints. Handlers return static mock data matching German sample content -- they prove the full MediatR pipeline works end-to-end.

Purpose: Complete the Application layer CQRS structure so Plan 04 can create thin Azure Functions triggers that dispatch to these handlers.
Output: Commands/ and Queries/ directories with 10 complete command/query feature folders.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-architecture-api-skeleton/04-CONTEXT.md
@.planning/phases/04-backend-architecture-api-skeleton/04-RESEARCH.md
@.planning/phases/04-backend-architecture-api-skeleton/04-01-SUMMARY.md

<interfaces>
<!-- Plan 01 types used by all commands/queries/handlers -->

From api/src/WissensHub.Application/Common/ApiResponse.cs:
```csharp
public class ApiResponse<T>
{
    public bool Success { get; init; }
    public T? Data { get; init; }
    public List<ApiError>? Errors { get; init; }
    public static ApiResponse<T> Ok(T data) => new() { Success = true, Data = data };
    public static ApiResponse<T> Fail(List<ApiError> errors) => new() { Success = false, Errors = errors };
    public static ApiResponse<T> Fail(string message) => new() { Success = false, Errors = [new(null, message)] };
}
public record ApiError(string? Field, string Message);
```

From api/src/WissensHub.Application/Common/Attributes/RequireGroupAttribute.cs:
```csharp
[AttributeUsage(AttributeTargets.Class, AllowMultiple = true)]
public class RequireGroupAttribute(string groupName) : Attribute
{
    public string GroupName { get; } = groupName;
}
```

From api/src/WissensHub.Application/Interfaces/ICurrentUser.cs:
```csharp
public interface ICurrentUser
{
    string UserId { get; }
    string DisplayName { get; }
    string Email { get; }
    IReadOnlyList<string> Groups { get; }
    bool IsInGroup(string groupName);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 4 command feature folders (MarkAsRead, FlagArticle, ToggleFavorite, ApproveArticle)</name>
  <files>
    api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs,
    api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadHandler.cs,
    api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadValidator.cs,
    api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleCommand.cs,
    api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleHandler.cs,
    api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleValidator.cs,
    api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteCommand.cs,
    api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteHandler.cs,
    api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteValidator.cs,
    api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs,
    api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleHandler.cs,
    api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleValidator.cs
  </files>
  <read_first>
    api/src/WissensHub.Application/Common/ApiResponse.cs,
    api/src/WissensHub.Application/Common/Attributes/RequireGroupAttribute.cs,
    api/src/WissensHub.Application/Interfaces/ICurrentUser.cs,
    api/src/WissensHub.Domain/Entities/ReadConfirmation.cs,
    api/src/WissensHub.Domain/Entities/ArticleFlag.cs,
    api/src/WissensHub.Domain/Entities/Favorite.cs,
    api/src/WissensHub.Domain/Entities/ApprovalHistory.cs
  </read_first>
  <action>
    Create 4 command feature folders under `api/src/WissensHub.Application/Commands/`. Each folder has 3 files: Command, Handler, Validator. All use file-scoped namespaces and primary constructors.

    All DTOs used as response types are simple records defined in the same Command file (co-located).

    **1. MarkAsRead (API-03: POST /api/articles/{pageId}/read)**

    `MarkAsReadCommand.cs`:
    ```
    namespace WissensHub.Application.Commands.MarkAsRead;
    using MediatR;
    using WissensHub.Application.Common;

    public record MarkAsReadCommand(int PageId) : IRequest<ApiResponse<ReadConfirmationDto>>;
    public record ReadConfirmationDto(int Id, int PageId, string UserId, string UserDisplayName, DateTime ReadDate, int ContentVersion);
    ```

    `MarkAsReadHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Returns mock data:
      ```csharp
      var dto = new ReadConfirmationDto(
          Id: 1, PageId: request.PageId, UserId: currentUser.UserId,
          UserDisplayName: currentUser.DisplayName, ReadDate: DateTime.UtcNow, ContentVersion: 1);
      return ApiResponse<ReadConfirmationDto>.Ok(dto);
      ```
    - Add comment: `// Feature phase: replace with real repo operations + db.SaveChangesAsync(ct)`

    `MarkAsReadValidator.cs`:
    - Extends `AbstractValidator<MarkAsReadCommand>`
    - Rule: `RuleFor(x => x.PageId).GreaterThan(0).WithMessage("PageId must be greater than 0");`

    **2. FlagArticle (API-04: POST /api/articles/{pageId}/flag)**

    `FlagArticleCommand.cs`:
    ```
    public record FlagArticleCommand(int PageId, string Reason) : IRequest<ApiResponse<ArticleFlagDto>>;
    public record ArticleFlagDto(int Id, int PageId, string UserId, string UserDisplayName, string Reason, DateTime FlaggedDate, bool IsResolved);
    ```

    `FlagArticleHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Mock data with German content:
      ```csharp
      var dto = new ArticleFlagDto(
          Id: 1, PageId: request.PageId, UserId: currentUser.UserId,
          UserDisplayName: currentUser.DisplayName, Reason: request.Reason,
          FlaggedDate: DateTime.UtcNow, IsResolved: false);
      return ApiResponse<ArticleFlagDto>.Ok(dto);
      ```

    `FlagArticleValidator.cs`:
    - Rules: `RuleFor(x => x.PageId).GreaterThan(0);`
    - `RuleFor(x => x.Reason).NotEmpty().WithMessage("Reason is required").MaximumLength(500).WithMessage("Reason must not exceed 500 characters");`

    **3. ToggleFavorite (API-08: POST /api/favorites/{pageId})**

    `ToggleFavoriteCommand.cs`:
    ```
    public record ToggleFavoriteCommand(int PageId) : IRequest<ApiResponse<ToggleFavoriteDto>>;
    public record ToggleFavoriteDto(int PageId, bool IsFavorited);
    ```

    `ToggleFavoriteHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Mock: returns `ToggleFavoriteDto(PageId: request.PageId, IsFavorited: true)`

    `ToggleFavoriteValidator.cs`:
    - Rule: `RuleFor(x => x.PageId).GreaterThan(0);`

    **4. ApproveArticle (API-06: POST /api/articles/{pageId}/approve)**

    `ApproveArticleCommand.cs`:
    ```
    [RequireGroup("WissensHub Reviewers")]
    public record ApproveArticleCommand(int PageId, string Action, string? Comment)
        : IRequest<ApiResponse<ApprovalHistoryDto>>;
    public record ApprovalHistoryDto(int Id, int PageId, string Action, string ActionBy, string ActionByDisplayName, DateTime ActionDate, string? Comment);
    ```

    `ApproveArticleHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Mock:
      ```csharp
      var dto = new ApprovalHistoryDto(
          Id: 1, PageId: request.PageId, Action: request.Action,
          ActionBy: currentUser.UserId, ActionByDisplayName: currentUser.DisplayName,
          ActionDate: DateTime.UtcNow, Comment: request.Comment);
      return ApiResponse<ApprovalHistoryDto>.Ok(dto);
      ```

    `ApproveArticleValidator.cs`:
    - Rules:
      - `RuleFor(x => x.PageId).GreaterThan(0);`
      - `RuleFor(x => x.Action).NotEmpty().Must(a => a is "Approved" or "Rejected").WithMessage("Action must be 'Approved' or 'Rejected'");`
      - `RuleFor(x => x.Comment).NotEmpty().When(x => x.Action == "Rejected").WithMessage("Comment is required when rejecting");`
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && dotnet build api/src/WissensHub.Application/WissensHub.Application.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs contains `record MarkAsReadCommand(int PageId) : IRequest<ApiResponse<ReadConfirmationDto>>`
    - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadValidator.cs contains `AbstractValidator<MarkAsReadCommand>`
    - api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadValidator.cs contains `GreaterThan(0)`
    - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleCommand.cs contains `record FlagArticleCommand(int PageId, string Reason)`
    - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleValidator.cs contains `NotEmpty()`
    - api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleValidator.cs contains `MaximumLength(500)`
    - api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteCommand.cs contains `record ToggleFavoriteCommand(int PageId)`
    - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs contains `[RequireGroup("WissensHub Reviewers")]`
    - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs contains `record ApproveArticleCommand(int PageId, string Action, string? Comment)`
    - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleValidator.cs contains `"Approved" or "Rejected"`
    - api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleValidator.cs contains `When(x => x.Action == "Rejected")`
    - All 4 handler files contain `ApiResponse<` and `.Ok(`
    - All 4 handler files contain comment about feature phase replacement
    - dotnet build of WissensHub.Application succeeds with 0 errors
  </acceptance_criteria>
  <done>4 command feature folders with Command/Handler/Validator triplets, each handler returns mock data through ApiResponse envelope, validators enforce meaningful rules, ApproveArticle has RequireGroup attribute</done>
</task>

<task type="auto">
  <name>Task 2: Create all 6 query feature folders (GetArticleStatus, GetUnreadArticles, GetReadStats, GetFavorites, GetDashboardStats, GetAdminReports)</name>
  <files>
    api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusQuery.cs,
    api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusHandler.cs,
    api/src/WissensHub.Application/Queries/GetArticleStatus/ArticleStatusDto.cs,
    api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesQuery.cs,
    api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesHandler.cs,
    api/src/WissensHub.Application/Queries/GetUnreadArticles/UnreadArticleDto.cs,
    api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsQuery.cs,
    api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsHandler.cs,
    api/src/WissensHub.Application/Queries/GetReadStats/ReadStatsDto.cs,
    api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesQuery.cs,
    api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesHandler.cs,
    api/src/WissensHub.Application/Queries/GetFavorites/FavoriteDto.cs,
    api/src/WissensHub.Application/Queries/GetDashboardStats/GetDashboardStatsQuery.cs,
    api/src/WissensHub.Application/Queries/GetDashboardStats/GetDashboardStatsHandler.cs,
    api/src/WissensHub.Application/Queries/GetDashboardStats/DashboardStatsDto.cs,
    api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsQuery.cs,
    api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsHandler.cs,
    api/src/WissensHub.Application/Queries/GetAdminReports/AdminReportDto.cs
  </files>
  <read_first>
    api/src/WissensHub.Application/Common/ApiResponse.cs,
    api/src/WissensHub.Application/Common/Attributes/RequireGroupAttribute.cs,
    api/src/WissensHub.Application/Interfaces/ICurrentUser.cs,
    api/src/WissensHub.Domain/Entities/ArticleMetadata.cs,
    api/src/WissensHub.Domain/Entities/ReadConfirmation.cs
  </read_first>
  <action>
    Create 6 query feature folders under `api/src/WissensHub.Application/Queries/`. Each folder has Query, Handler, and DTO files. All use file-scoped namespaces, primary constructors, and return `Task<ApiResponse<TDto>>`. Handlers return static German mock data.

    **1. GetArticleStatus (API-01: GET /api/articles/{pageId}/status)**

    `ArticleStatusDto.cs`:
    ```
    namespace WissensHub.Application.Queries.GetArticleStatus;
    public record ArticleStatusDto(
        int PageId, string Status, string? Category, bool IsMandatory,
        DateTime UpdatedAt, int ContentVersion,
        bool HasRead, DateTime? ReadDate, List<string> TargetGroups);
    ```

    `GetArticleStatusQuery.cs`:
    ```
    public record GetArticleStatusQuery(int PageId) : IRequest<ApiResponse<ArticleStatusDto>>;
    ```

    `GetArticleStatusHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Return mock:
      ```csharp
      var dto = new ArticleStatusDto(
          PageId: request.PageId, Status: "Published", Category: "IT-Sicherheit",
          IsMandatory: true, UpdatedAt: DateTime.UtcNow.AddDays(-5), ContentVersion: 2,
          HasRead: false, ReadDate: null, TargetGroups: ["Alle Mitarbeiter"]);
      return Task.FromResult(ApiResponse<ArticleStatusDto>.Ok(dto));
      ```

    **2. GetUnreadArticles (API-02: GET /api/articles/unread)**

    `UnreadArticleDto.cs`:
    ```
    public record UnreadArticleDto(int PageId, string Title, string Category, bool IsMandatory, DateTime UpdatedAt);
    ```

    `GetUnreadArticlesQuery.cs`:
    ```
    public record GetUnreadArticlesQuery() : IRequest<ApiResponse<List<UnreadArticleDto>>>;
    ```

    `GetUnreadArticlesHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Return mock list of 3 unread articles with German titles:
      ```csharp
      var articles = new List<UnreadArticleDto>
      {
          new(PageId: 1, Title: "IT-Sicherheitsrichtlinie 2024", Category: "IT-Sicherheit", IsMandatory: true, UpdatedAt: DateTime.UtcNow.AddDays(-2)),
          new(PageId: 3, Title: "Datenschutz-Grundlagen", Category: "Datenschutz", IsMandatory: true, UpdatedAt: DateTime.UtcNow.AddDays(-7)),
          new(PageId: 5, Title: "Passwort-Richtlinie", Category: "IT-Sicherheit", IsMandatory: true, UpdatedAt: DateTime.UtcNow.AddDays(-14))
      };
      return Task.FromResult(ApiResponse<List<UnreadArticleDto>>.Ok(articles));
      ```

    **3. GetReadStats (API-05: GET /api/articles/{pageId}/readstats)**

    `ReadStatsDto.cs`:
    ```
    public record ReadStatsDto(int PageId, int TotalTargetUsers, int ReadCount, int UnreadCount, List<UserReadStatusDto> Users);
    public record UserReadStatusDto(string UserId, string DisplayName, bool HasRead, DateTime? ReadDate);
    ```

    `GetReadStatsQuery.cs`:
    ```
    [RequireGroup("WissensHub Reviewers")]
    public record GetReadStatsQuery(int PageId) : IRequest<ApiResponse<ReadStatsDto>>;
    ```

    `GetReadStatsHandler.cs`:
    - Return mock with 3 users, 2 read, 1 unread:
      ```csharp
      var users = new List<UserReadStatusDto>
      {
          new("user-1", "Max Mustermann", true, DateTime.UtcNow.AddDays(-3)),
          new("user-2", "Erika Musterfrau", true, DateTime.UtcNow.AddDays(-1)),
          new("user-3", "Hans Schmidt", false, null)
      };
      var dto = new ReadStatsDto(PageId: request.PageId, TotalTargetUsers: 3, ReadCount: 2, UnreadCount: 1, Users: users);
      return Task.FromResult(ApiResponse<ReadStatsDto>.Ok(dto));
      ```

    **4. GetFavorites (API-07: GET /api/favorites)**

    `FavoriteDto.cs`:
    ```
    public record FavoriteDto(int PageId, string Title, string Category, DateTime FavoritedAt);
    ```

    `GetFavoritesQuery.cs`:
    ```
    public record GetFavoritesQuery() : IRequest<ApiResponse<List<FavoriteDto>>>;
    ```

    `GetFavoritesHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Return mock list of 2 favorites with German titles:
      ```csharp
      var favorites = new List<FavoriteDto>
      {
          new(PageId: 2, Title: "Onboarding-Leitfaden", Category: "Personalwesen", FavoritedAt: DateTime.UtcNow.AddDays(-10)),
          new(PageId: 4, Title: "Reisekostenrichtlinie", Category: "Finanzen", FavoritedAt: DateTime.UtcNow.AddDays(-5))
      };
      return Task.FromResult(ApiResponse<List<FavoriteDto>>.Ok(favorites));
      ```

    **5. GetDashboardStats (API-09: GET /api/dashboard/stats)**

    `DashboardStatsDto.cs`:
    ```
    public record DashboardStatsDto(int UnreadCount, int FavoritesCount, int PendingReviewsCount);
    ```

    `GetDashboardStatsQuery.cs`:
    ```
    public record GetDashboardStatsQuery() : IRequest<ApiResponse<DashboardStatsDto>>;
    ```

    `GetDashboardStatsHandler.cs`:
    - Primary constructor: `(ICurrentUser currentUser)`
    - Return mock: `DashboardStatsDto(UnreadCount: 3, FavoritesCount: 2, PendingReviewsCount: 1)`

    **6. GetAdminReports (API-10: GET /api/admin/reports)**

    `AdminReportDto.cs`:
    ```
    public record AdminReportDto(List<ArticleReportDto> Articles, int TotalArticles, int PublishedCount, int DraftCount, int InReviewCount);
    public record ArticleReportDto(int PageId, string Title, string Status, string Category, int ReadCount, int TargetUserCount, int FlagCount, DateTime LastUpdated);
    ```

    `GetAdminReportsQuery.cs`:
    ```
    [RequireGroup("WissensHub Owners")]
    public record GetAdminReportsQuery() : IRequest<ApiResponse<AdminReportDto>>;
    ```

    `GetAdminReportsHandler.cs`:
    - Return mock report with 3 articles:
      ```csharp
      var articles = new List<ArticleReportDto>
      {
          new(PageId: 1, Title: "IT-Sicherheitsrichtlinie 2024", Status: "Published", Category: "IT-Sicherheit", ReadCount: 42, TargetUserCount: 50, FlagCount: 0, LastUpdated: DateTime.UtcNow.AddDays(-5)),
          new(PageId: 2, Title: "Onboarding-Leitfaden", Status: "Published", Category: "Personalwesen", ReadCount: 28, TargetUserCount: 30, FlagCount: 1, LastUpdated: DateTime.UtcNow.AddDays(-15)),
          new(PageId: 6, Title: "Neue Arbeitszeitregelung", Status: "InReview", Category: "Personalwesen", ReadCount: 0, TargetUserCount: 50, FlagCount: 0, LastUpdated: DateTime.UtcNow.AddDays(-1))
      };
      var dto = new AdminReportDto(Articles: articles, TotalArticles: 10, PublishedCount: 7, DraftCount: 2, InReviewCount: 1);
      return Task.FromResult(ApiResponse<AdminReportDto>.Ok(dto));
      ```
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && dotnet build api/src/WissensHub.Application/WissensHub.Application.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusQuery.cs contains `record GetArticleStatusQuery(int PageId) : IRequest<ApiResponse<ArticleStatusDto>>`
    - api/src/WissensHub.Application/Queries/GetArticleStatus/ArticleStatusDto.cs contains `record ArticleStatusDto(`
    - api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusHandler.cs contains `IT-Sicherheit`
    - api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesQuery.cs contains `record GetUnreadArticlesQuery() : IRequest<ApiResponse<List<UnreadArticleDto>>>`
    - api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesHandler.cs contains `Datenschutz-Grundlagen`
    - api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsQuery.cs contains `[RequireGroup("WissensHub Reviewers")]`
    - api/src/WissensHub.Application/Queries/GetReadStats/ReadStatsDto.cs contains `record UserReadStatusDto`
    - api/src/WissensHub.Application/Queries/GetFavorites/FavoriteDto.cs contains `record FavoriteDto(`
    - api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesHandler.cs contains `Onboarding-Leitfaden`
    - api/src/WissensHub.Application/Queries/GetDashboardStats/DashboardStatsDto.cs contains `record DashboardStatsDto(int UnreadCount, int FavoritesCount, int PendingReviewsCount)`
    - api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsQuery.cs contains `[RequireGroup("WissensHub Owners")]`
    - api/src/WissensHub.Application/Queries/GetAdminReports/AdminReportDto.cs contains `record ArticleReportDto`
    - All 6 handler files contain `Task.FromResult(ApiResponse<`
    - dotnet build of WissensHub.Application succeeds with 0 errors
  </acceptance_criteria>
  <done>6 query feature folders with Query/Handler/DTO triplets, German mock data, RequireGroup on GetReadStats and GetAdminReports, all DTOs are records, compilation succeeds</done>
</task>

</tasks>

<verification>
Application project compiles with all commands and queries:
```bash
dotnet build api/src/WissensHub.Application/WissensHub.Application.csproj
```
Expected: Build succeeded. 0 Error(s).
Verify handler count: 10 handlers (4 command + 6 query) all implement IRequestHandler.
</verification>

<success_criteria>
- 30 files created across Commands/ and Queries/ directories (10 feature folders x 3 files each)
- All 4 command records implement IRequest<ApiResponse<TDto>>
- All 4 validators extend AbstractValidator with meaningful validation rules
- All 6 query records implement IRequest<ApiResponse<TDto>>
- ApproveArticleCommand has [RequireGroup("WissensHub Reviewers")]
- GetReadStatsQuery has [RequireGroup("WissensHub Reviewers")]
- GetAdminReportsQuery has [RequireGroup("WissensHub Owners")]
- All handlers return static mock data with German content
- WissensHub.Application project compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-architecture-api-skeleton/04-03-SUMMARY.md`
</output>
