---
phase: 08-unread-badge-application-customizer
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - BADGE-01
  - BADGE-02
  - BADGE-03

must_haves:
  truths:
    - "Build passes with 0 errors confirming all TypeScript compiles"
    - "All tests pass including new UnreadBadgeHeader and UnreadFlyoutPanel tests"
    - "Workbench inspection confirms bell icon visible in header area"
    - "Workbench inspection confirms badge shows unread count"
    - "Workbench inspection confirms flyout panel opens with article list"
  artifacts: []
  key_links: []
---

<objective>
Verify all 3 Phase 8 requirements (BADGE-01, BADGE-02, BADGE-03) via build verification, test suite execution, and workbench visual inspection.

Purpose: Confirm the Unread Badge Application Customizer works correctly before moving to Phase 9.
Output: Verification results documenting build, test, and visual inspection outcomes.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-unread-badge-application-customizer/08-CONTEXT.md
@.planning/phases/08-unread-badge-application-customizer/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 8 requirements via build, tests, and workbench inspection</name>
  <files></files>
  <read_first>
    .planning/phases/08-unread-badge-application-customizer/08-01-SUMMARY.md
  </read_first>
  <action>
Run build and test suite, then present verification checklist to user for approval.

1. Run `cd spfx && npx heft build --clean` and confirm 0 errors
2. Run `cd spfx && npx heft test --clean` and confirm all tests pass
3. Present the requirements checklist below for user sign-off

**Requirements checklist:**
- BADGE-01: UnreadBadgeHeader.tsx renders Icon with iconName="Ringer", badge span with count, positioned in PlaceholderName.Top. Badge hidden when count is 0. Count capped at "99+".
- BADGE-02: UnreadFlyoutPanel.tsx uses Panel with PanelType.smallFixedFar, isLightDismiss=true. Mandatory articles sorted to top with "Pflichtartikel" badge. Each item shows title, colored category badge, relative date. Max 10 items with "Alle N anzeigen" link. Empty state with CheckMark icon and "Alle Artikel gelesen!".
- BADGE-03: Article click navigates via window.location.href and closes panel. ReadStatusSection.tsx dispatches CustomEvent('wissenshub:article-read') on successful mark-as-read.

**Workbench note:** Application Customizers cannot run on the local workbench. They require `npm start` which loads a real SharePoint page via serve.json pageUrl. If tenant access is unavailable, verify via build output and code review.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean 2>&1 | tail -5 && npx heft test --clean 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 BADGE requirements verified and approved. Build passes with 0 errors, all tests pass, user confirms implementation via code review or workbench testing.</done>
  <acceptance_criteria>
    - `cd spfx && npx heft build --clean` exits with code 0
    - `cd spfx && npx heft test --clean` exits with code 0
    - All 3 BADGE requirements confirmed via code inspection or workbench testing
  </acceptance_criteria>
</task>

</tasks>

<verification>
All 3 Phase 8 requirements verified:
1. BADGE-01: Notification icon with unread count in header
2. BADGE-02: Flyout panel with unread article summaries on click
3. BADGE-03: Article click navigates to article page
</verification>

<success_criteria>
- Build passes with 0 errors
- All tests pass (existing + new)
- All 3 BADGE requirements approved via inspection
- Phase 8 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-unread-badge-application-customizer/08-02-SUMMARY.md`
</output>
