---
phase: 05-dashboard-web-part
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - spfx/package.json
  - spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08, DASH-09, DASH-10, RMND-01]

must_haves:
  truths:
    - "Test infrastructure is installed and npx heft test --clean runs without errors"
    - "Test stub files exist for Dashboard, ArticleCard, StatsBar, and FilterBar components"
    - "Each test file has at least one placeholder test that passes"
  artifacts:
    - path: "spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx"
      provides: "Integration test stubs for DASH-01, DASH-08"
      min_lines: 15
    - path: "spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx"
      provides: "Card rendering test stubs for DASH-01, DASH-02, DASH-03, DASH-04, RMND-01"
      min_lines: 15
    - path: "spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx"
      provides: "Stats bar rendering test stubs for DASH-05, DASH-10"
      min_lines: 15
    - path: "spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx"
      provides: "Filter logic test stubs for DASH-06, DASH-07"
      min_lines: 15
  key_links:
    - from: "spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx"
      to: "spfx/src/webparts/dashboard/components/ArticleCard.tsx"
      via: "import for rendering tests (file created in Plan 01)"
      pattern: "ArticleCard"
    - from: "spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx"
      to: "spfx/src/webparts/dashboard/components/StatsBar.tsx"
      via: "import for rendering tests (file created in Plan 02)"
      pattern: "StatsBar"
---

<objective>
Create the Wave 0 test infrastructure for Phase 5: install React Testing Library, create test stub files for Dashboard, ArticleCard, StatsBar, and FilterBar components. Each stub contains placeholder tests that describe the expected behaviors (per VALIDATION.md) but use trivial assertions so they pass immediately. Later plans will fill in real implementations as components are built.

Purpose: Satisfy the Nyquist rule -- every subsequent task can verify with `npx heft test --clean` and get real behavioral feedback
Output: 4 test stub files, @testing-library/react installed, heft test passes green
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-web-part/05-CONTEXT.md
@.planning/phases/05-dashboard-web-part/05-VALIDATION.md

<interfaces>
<!-- Test infrastructure context -->

SPFx uses Heft build rig (@microsoft/spfx-web-build-rig) which includes Jest.
React version: 17.0.1 (requires @testing-library/react v12 or v13, NOT v14+).
@types/heft-jest is already installed (provides Jest type definitions).
No existing test files in the spfx/src directory.
Jest config inherited from Heft rig -- no explicit jest.config.js needed.

From spfx/src/shared/models/domain/IArticlePage.ts:
```typescript
export interface IArticlePage {
  id: number; title: string; category: string; status: ArticleStatus;
  isMandatory: boolean; targetGroups: string[]; modifiedDate: Date;
  author: IUser; url: string;
}
```

From spfx/src/shared/models/domain/types.ts:
```typescript
export type ArticleStatus = 'Draft' | 'InReview' | 'Published' | 'Archived';
export type UserRole = 'reader' | 'editor' | 'reviewer' | 'admin';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and create test stub files</name>
  <files>
    spfx/package.json
    spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx
  </files>
  <read_first>
    spfx/package.json
    spfx/config/rig.json
  </read_first>
  <action>
Two steps: install test library, then create 4 test stub files.

**Step 1: Install @testing-library/react for React 17:**
```bash
cd spfx && npm install --save-dev @testing-library/react@^13.4.0 @testing-library/jest-dom@^5.17.0
```
Use v13 of @testing-library/react (last version supporting React 17). Use v5 of jest-dom (v6 requires Jest 28+, SPFx Heft uses Jest 27).

**Step 2: Create `__tests__/` directory and 4 stub files.**

Each test file follows this pattern:
- Import React
- Import the component (use a try/catch or conditional comment since components do not exist yet)
- Define `describe` blocks mapping to VALIDATION.md requirements
- Each `it()` block has a descriptive name matching expected behavior but uses `expect(true).toBe(true)` as placeholder
- Add a `// TODO: implement after Plan XX creates the component` comment

**2a. `__tests__/Dashboard.test.tsx`:**
```typescript
import * as React from 'react';

// Component import will be available after Plan 01
// import { Dashboard } from '../Dashboard';

describe('Dashboard (DASH-01, DASH-08)', () => {
  it('should render article cards in a responsive grid', () => {
    // TODO: implement after Plan 01 creates Dashboard.tsx
    expect(true).toBe(true);
  });

  it('should toggle between card and list view', () => {
    // TODO: implement after Plan 01 creates view toggle
    expect(true).toBe(true);
  });

  it('should display articles sorted by modified date descending', () => {
    // TODO: implement after Plan 01 creates sorting logic
    expect(true).toBe(true);
  });

  it('should show shimmer loading state while articles load', () => {
    // TODO: implement after Plan 01 creates loading state
    expect(true).toBe(true);
  });

  it('should show empty state when no articles exist', () => {
    // TODO: implement after Plan 01 creates EmptyState
    expect(true).toBe(true);
  });
});
```

**2b. `__tests__/ArticleCard.test.tsx`:**
```typescript
import * as React from 'react';

// Component import will be available after Plan 01
// import { ArticleCard } from '../ArticleCard';

describe('ArticleCard (DASH-01, DASH-02, DASH-03, DASH-04, RMND-01)', () => {
  it('should render category badge with deterministic color', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should render title, author, and relative date', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should show unread indicator (blue left border + dot) for unread articles', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should NOT show unread indicator for read articles', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should show Pflichtartikel badge for mandatory articles', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should toggle favorite star on click with stopPropagation', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should call onClick with article URL when card is clicked', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });

  it('should be keyboard accessible (Enter/Space triggers click)', () => {
    // TODO: implement after Plan 01 creates ArticleCard
    expect(true).toBe(true);
  });
});
```

**2c. `__tests__/StatsBar.test.tsx`:**
```typescript
import * as React from 'react';

// Component import will be available after Plan 02
// import { StatsBar } from '../StatsBar';

describe('StatsBar (DASH-05, DASH-10)', () => {
  it('should render unread count with Ungelesen label', () => {
    // TODO: implement after Plan 02 creates StatsBar
    expect(true).toBe(true);
  });

  it('should render favorites count with Favoriten label', () => {
    // TODO: implement after Plan 02 creates StatsBar
    expect(true).toBe(true);
  });

  it('should render Offen stat only inside RoleGate for reviewer role', () => {
    // TODO: implement after Plan 02 creates StatsBar with RoleGate
    expect(true).toBe(true);
  });

  it('should NOT render Offen stat for reader or editor roles', () => {
    // TODO: implement after Plan 02 creates StatsBar with RoleGate
    expect(true).toBe(true);
  });

  it('should highlight active stat filter with visual indicator', () => {
    // TODO: implement after Plan 02 creates StatsBar
    expect(true).toBe(true);
  });

  it('should toggle stat filter on click (activate/deactivate)', () => {
    // TODO: implement after Plan 02 creates StatsBar
    expect(true).toBe(true);
  });

  it('should have aria-pressed attribute for accessibility', () => {
    // TODO: implement after Plan 02 creates StatsBar
    expect(true).toBe(true);
  });
});
```

**2d. `__tests__/FilterBar.test.tsx`:**
```typescript
import * as React from 'react';

// Component import will be available after Plan 02
// import { FilterBar } from '../FilterBar';

describe('FilterBar (DASH-06, DASH-07)', () => {
  it('should render search input with placeholder text', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should render category dropdown with provided categories', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should render status dropdown with German labels', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should render target group dropdown', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should show active filter pills for non-empty filters', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should call onClearAllFilters when reset link is clicked', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should render view toggle buttons for card and list modes', () => {
    // TODO: implement after Plan 02 creates FilterBar
    expect(true).toBe(true);
  });

  it('should render Neuer Artikel button inside RoleGate for editors', () => {
    // TODO: implement after Plan 03 adds RoleGate button
    expect(true).toBe(true);
  });
});
```

**IMPORTANT:** All test files use `expect(true).toBe(true)` as placeholder assertions. This ensures `npx heft test --clean` passes green immediately. As each plan is executed, the executor should uncomment the component import and replace placeholder assertions with real rendering tests using @testing-library/react.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -20</automated>
  </verify>
  <done>4 test stub files exist in __tests__/ directory. @testing-library/react and @testing-library/jest-dom are installed. `npx heft test --clean` passes with all placeholder tests green.</done>
</task>

</tasks>

<verification>
1. `cd spfx && npx heft test --clean` -- all tests pass green
2. `cd spfx && npx heft build --clean` -- build still passes (no regressions from new devDependencies)
3. All 4 test files exist: Dashboard.test.tsx, ArticleCard.test.tsx, StatsBar.test.tsx, FilterBar.test.tsx
4. @testing-library/react and @testing-library/jest-dom appear in package.json devDependencies
</verification>

<success_criteria>
- `npx heft test --clean` exits 0 with all test stubs passing
- `npx heft build --clean` exits 0 (no regressions)
- 4 test stub files exist with describe blocks mapping to VALIDATION.md requirements
- @testing-library/react v13 and @testing-library/jest-dom v5 installed
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-web-part/05-00-SUMMARY.md`
</output>
