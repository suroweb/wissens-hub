---
phase: 10-caching-telemetry-ux-polish-i18n
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified: []
autonomous: false
requirements:
  - CACH-01
  - CACH-02
  - CACH-03
  - CACH-04
  - TELE-01
  - TELE-02
  - TELE-03
  - TELE-04
  - TELE-05
  - TELE-06
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - I18N-01
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "Build produces 0 errors"
    - "All tests pass (121+ tests)"
    - "Every web part renders in workbench without errors"
    - "ErrorBoundary catches thrown errors and shows recovery UI"
    - "Shimmer loading skeletons appear during data fetch"
    - "German strings display correctly as default language"
    - "All interactive elements have keyboard focus indicators"
  artifacts: []
  key_links: []
---

<objective>
Verify all 18 Phase 10 requirements are met via build, test suite, and workbench visual inspection.

Purpose: Final quality gate before marking Phase 10 complete. Validates caching, telemetry, UX polish, and i18n all work together.

Output: Phase 10 verified and ready for STATE.md update.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-CONTEXT.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-01-SUMMARY.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-02-SUMMARY.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-03-SUMMARY.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification -- build, tests, and structural checks</name>
  <files></files>
  <read_first>
    .planning/phases/10-caching-telemetry-ux-polish-i18n/10-CONTEXT.md,
    .planning/phases/10-caching-telemetry-ux-polish-i18n/10-01-SUMMARY.md,
    .planning/phases/10-caching-telemetry-ux-polish-i18n/10-02-SUMMARY.md,
    .planning/phases/10-caching-telemetry-ux-polish-i18n/10-03-SUMMARY.md,
    .planning/phases/10-caching-telemetry-ux-polish-i18n/10-04-SUMMARY.md
  </read_first>
  <action>
Run the following automated checks and record results:

**1. Build verification:**
```bash
cd spfx && npx heft build --clean
```
Must exit 0 with 0 errors.

**2. Test suite:**
```bash
cd spfx && npx heft test --clean
```
Must show all tests passing (121+ tests).

**3. Structural checks (grep verification of all 18 requirements):**

CACH-01: `grep "Caching" spfx/src/shared/context/pnpSetup.ts` -- confirms PnPjs session caching
CACH-02: `grep "class CacheService" spfx/src/shared/services/CacheService.ts` -- confirms in-memory cache
CACH-03: `grep -r "services.cache.get" spfx/src/shared/hooks/queries/ | wc -l` -- should be 13
CACH-04: `grep -r "services.cache.invalidate" spfx/src/shared/hooks/commands/ | wc -l` -- should be 13+
TELE-01: `grep "ApplicationInsights" spfx/src/shared/services/TelemetryService.ts` -- confirms App Insights
TELE-02: `grep "disableFetchTracking" spfx/src/shared/services/TelemetryService.ts` -- confirms cost-safe config
TELE-03: `grep -r "services.telemetry.trackEvent" spfx/src/shared/hooks/commands/ | wc -l` -- should be 26+
TELE-04: `grep "ConsoleTelemetryService" spfx/src/shared/services/TelemetryService.ts` -- confirms dev fallback
TELE-05: `grep "ErrorBoundary" spfx/src/webparts/dashboard/DashboardWebPart.ts` -- confirms Error Boundary wrapping
TELE-06: `grep "ToastProvider" spfx/src/webparts/dashboard/DashboardWebPart.ts` -- confirms toast wiring
UX-01: `grep -r "optimistic\|setLocal" spfx/src/webparts/articleSidebar/ spfx/src/webparts/dashboard/` -- confirms optimistic UI
UX-02: `grep -r "Shimmer\|ShimmerCard\|ShimmerTable" spfx/src/webparts/*/components/*.tsx` -- confirms shimmer usage
UX-03: `grep -r "debounce\|useDebounce\|setTimeout.*search" spfx/src/` -- confirms debounced search
UX-04: `grep -r "max-width\|grid-template-columns\|@media" spfx/src/webparts/dashboard/components/Dashboard.module.scss` -- confirms responsive CSS
UX-05: `grep -r "aria-label\|role=" spfx/src/webparts/dashboard/components/ArticleCard.tsx` -- confirms ARIA
I18N-01: `ls spfx/src/shared/loc/de-de.js spfx/src/webparts/*/loc/de-de.js spfx/src/extensions/*/loc/de-de.js` -- confirms German files exist
I18N-02: `ls spfx/src/shared/loc/en-us.js` -- confirms English files exist
I18N-03: `grep "SharedStrings" spfx/config/config.json` -- confirms registration

Record a verification matrix showing PASS/FAIL for each requirement.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx heft build --clean exits with 0 errors
    - npx heft test --clean shows all tests passing
    - All 18 structural grep checks return expected results
    - Verification matrix shows PASS for all 18 requirements
  </acceptance_criteria>
  <done>Build and tests pass. All 18 requirements verified via structural checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Workbench visual verification of all Phase 10 features</name>
  <files></files>
  <action>
Present verification instructions to user for workbench visual inspection of caching, telemetry, UX polish, and i18n. Phase 10 adds four cross-cutting production quality layers:
1. Caching: PnPjs session cache + in-memory CacheService with stale-while-revalidate + cache invalidation
2. Telemetry: Application Insights with 9 custom events + ConsoleTelemetryService for local dev
3. UX Polish: Error Boundaries, Toast notifications, Shimmer loading skeletons, Responsive CSS, ARIA accessibility
4. i18n: German (default) and English localization with SPFx built-in loc system

Verification steps:
1. Run `cd spfx && npm start` to open the local workbench
2. Add the Dashboard web part -- verify shimmer skeletons appear briefly, German labels display, Tab navigation works on cards, favorite toggle is instant with console telemetry event
3. Add the Article Sidebar web part -- verify German metadata labels, mark-as-read is instant with console telemetry
4. Add the Freigabecenter web part -- verify German tab labels, shimmer on load
5. Add the Admin Panel web part -- verify German tab labels, shimmer on report table load
6. Resize browser window narrow -- verify Dashboard cards reflow 3-col to 2-col to 1-col
7. Check browser console for `[Telemetry] Event:` messages on any action
  </action>
  <verify>User confirms all features work correctly in workbench</verify>
  <acceptance_criteria>
    - User types "approved" confirming all visual checks pass
    - Shimmer loading states visible during data fetch
    - German labels display as default language
    - Console telemetry events appear on user actions
    - Responsive layout works at narrow widths
    - Keyboard navigation works on interactive elements
  </acceptance_criteria>
  <done>Phase 10 verified and approved by user via workbench visual inspection.</done>
</task>

</tasks>

<verification>
All 18 Phase 10 requirements verified via:
1. Automated build (0 errors)
2. Automated tests (all passing)
3. Structural grep checks (all 18 PASS)
4. Human visual inspection in workbench (shimmer, German labels, telemetry events, responsive, keyboard)
</verification>

<success_criteria>
Phase 10 complete: caching, telemetry, UX polish, and i18n all verified and approved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-caching-telemetry-ux-polish-i18n/10-05-SUMMARY.md`
</output>
