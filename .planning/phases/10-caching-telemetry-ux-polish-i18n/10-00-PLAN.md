---
phase: 10-caching-telemetry-ux-polish-i18n
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - spfx/src/shared/services/__tests__/CacheService.test.ts
  - spfx/src/shared/services/__tests__/TelemetryService.test.ts
  - spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx
  - spfx/src/shared/components/__tests__/ToastProvider.test.tsx
autonomous: true
requirements:
  - CACH-02
  - TELE-01
  - TELE-02
  - TELE-04
  - TELE-05
  - TELE-06

must_haves:
  truths:
    - "Test stub files exist and run without errors (0 assertions, 0 failures)"
    - "Each stub describes the behaviors it will validate once implementations exist"
  artifacts:
    - path: "spfx/src/shared/services/__tests__/CacheService.test.ts"
      provides: "Test stubs for CacheService get/set/invalidate/TTL"
      contains: "describe.*CacheService"
    - path: "spfx/src/shared/services/__tests__/TelemetryService.test.ts"
      provides: "Test stubs for ITelemetryService, AppInsightsTelemetryService, ConsoleTelemetryService"
      contains: "describe.*TelemetryService"
    - path: "spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx"
      provides: "Test stubs for ErrorBoundary catch and recovery"
      contains: "describe.*ErrorBoundary"
    - path: "spfx/src/shared/components/__tests__/ToastProvider.test.tsx"
      provides: "Test stubs for ToastProvider show/dismiss/auto-dismiss"
      contains: "describe.*ToastProvider"
  key_links: []
---

<objective>
Create Wave 0 test stub files required by the Nyquist validation strategy. These stubs define the test structure and placeholder tests for CacheService, TelemetryService, ErrorBoundary, and ToastProvider. Actual assertions will be filled in during Plans 01-03.

Purpose: Satisfies the Nyquist rule -- every task with `<automated>` verification must have a test file that exists before execution. Stubs allow `npx heft test --clean` to pass with 0 failures throughout the phase.

Output: 4 test stub files in `__tests__/` directories, each with `describe` blocks and `it.todo()` placeholders.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-CONTEXT.md
@.planning/phases/10-caching-telemetry-ux-polish-i18n/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 test stub files for Phase 10 infrastructure</name>
  <files>
    spfx/src/shared/services/__tests__/CacheService.test.ts,
    spfx/src/shared/services/__tests__/TelemetryService.test.ts,
    spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx,
    spfx/src/shared/components/__tests__/ToastProvider.test.tsx
  </files>
  <read_first>
    spfx/src/shared/services/__tests__,
    spfx/src/shared/components/__tests__
  </read_first>
  <action>
**Create 4 test stub files. Each file uses `describe` blocks with `it.todo()` placeholders. No imports of unwritten modules -- stubs must pass immediately.**

**Step 1: Create `spfx/src/shared/services/__tests__/CacheService.test.ts`:**
```typescript
describe('CacheService', () => {
  it.todo('returns undefined for cache miss');
  it.todo('returns stored value for cache hit within TTL');
  it.todo('returns undefined for expired entry and deletes key');
  it.todo('invalidates entries matching prefix pattern');
  it.todo('clear() removes all entries');
  it.todo('set() uses default TTL when none provided');
});
```

**Step 2: Create `spfx/src/shared/services/__tests__/TelemetryService.test.ts`:**
```typescript
describe('TelemetryService', () => {
  describe('ConsoleTelemetryService', () => {
    it.todo('trackEvent logs to console');
    it.todo('trackException logs error to console');
    it.todo('trackPageView logs page name to console');
  });

  describe('AppInsightsTelemetryService', () => {
    it.todo('initializes ApplicationInsights with connection string');
    it.todo('sets disableFetchTracking and disableAjaxTracking to true');
    it.todo('trackEvent calls appInsights.trackEvent');
    it.todo('trackException calls appInsights.trackException');
  });

  describe('createTelemetryService', () => {
    it.todo('returns ConsoleTelemetryService when connection string is empty');
    it.todo('returns AppInsightsTelemetryService when connection string is provided');
  });
});
```

**Step 3: Create `spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx`:**
```typescript
describe('ErrorBoundary', () => {
  it.todo('renders children when no error occurs');
  it.todo('renders fallback when child component throws');
  it.todo('calls telemetry.trackException on error');
  it.todo('passes componentStack in trackException properties');
});
```

**Step 4: Create `spfx/src/shared/components/__tests__/ToastProvider.test.tsx`:**
```typescript
describe('ToastProvider', () => {
  it.todo('renders children without toasts initially');
  it.todo('showToast adds a MessageBar to the DOM');
  it.todo('success toast uses MessageBarType.success');
  it.todo('error toast uses MessageBarType.error');
  it.todo('toast auto-dismisses after 5 seconds');
  it.todo('onDismiss removes the toast from DOM');
});
```

**Ensure each file has NO imports** -- only `describe` and `it.todo()`. This guarantees they pass immediately with `npx heft test --clean` (todo tests are reported as skipped, not failures).
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -10</automated>
  </verify>
  <done>4 test stub files exist in __tests__/ directories. `npx heft test --clean` passes with 0 failures (todo tests reported as skipped).</done>
</task>

</tasks>

<verification>
- `ls spfx/src/shared/services/__tests__/CacheService.test.ts spfx/src/shared/services/__tests__/TelemetryService.test.ts spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx spfx/src/shared/components/__tests__/ToastProvider.test.tsx` -- all 4 files exist
- `cd spfx && npx heft test --clean` -- passes with 0 failures
</verification>

<success_criteria>
4 test stub files created. Test suite passes with 0 failures. Wave 0 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-caching-telemetry-ux-polish-i18n/10-00-SUMMARY.md`
</output>
