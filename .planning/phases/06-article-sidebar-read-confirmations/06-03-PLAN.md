---
phase: 06-article-sidebar-read-confirmations
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false
requirements:
  - SIDE-01
  - SIDE-02
  - SIDE-03
  - SIDE-04
  - SIDE-05
  - SIDE-06
  - SIDE-07
  - SIDE-08
  - READ-01
  - READ-02
  - READ-03

must_haves:
  truths:
    - "All 11 phase requirements verified in workbench or via build/test"
    - "Article Sidebar renders complete UI in SPFx workbench"
    - "Optimistic UI for mark-as-read and favorite toggle works correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Article Sidebar in SPFx local workbench. All 11 requirements (SIDE-01 through SIDE-08, READ-01 through READ-03) should be demonstrable.

Purpose: Human verification that the sidebar looks correct, interactions work, and all sections render properly before marking the phase complete.
Output: Verified sidebar with all features confirmed working.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-article-sidebar-read-confirmations/06-CONTEXT.md
@.planning/phases/06-article-sidebar-read-confirmations/06-01-SUMMARY.md
@.planning/phases/06-article-sidebar-read-confirmations/06-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Workbench verification of complete Article Sidebar</name>
  <files></files>
  <action>
Run build and test commands to verify the sidebar compiles and tests pass, then present the workbench verification checklist to the user.

1. Run `cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean` and verify zero errors.
2. Run `cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean` and verify all tests pass.
3. Present the workbench verification checklist below to the user and wait for approval.

**What was built:**
Complete Article Sidebar web part with:
- Metadata section showing 6 fields with icons (SIDE-01)
- Mark-as-read with optimistic UI (SIDE-02)
- Read status display "Gelesen am [date]" or unread (SIDE-03)
- Flag-as-outdated dialog (SIDE-04)
- Favorite star toggle (SIDE-05)
- Dynamic table of contents (SIDE-06, may not render in workbench due to no CanvasZone)
- Version history link (SIDE-07)
- Editor-only "Metadaten bearbeiten" button (SIDE-08)
- Read confirmation with version tracking (READ-01, READ-02)
- Unread count contribution via mark-as-read (READ-03)

**How to verify in workbench:**
1. Start the workbench: `cd spfx && npx gulp serve`
2. Open browser to `https://localhost:4321/temp/workbench.html`
3. Add the "ArticleSidebar" web part to the workbench canvas

**Metadata section (SIDE-01):** Verify 6 metadata fields display with icons: Autor, Kategorie, Zuletzt aktualisiert, Version, Status, Zielgruppen. Verify German labels are used throughout.

**Read status (SIDE-02, SIDE-03, READ-01):** If article shows as unread, click "Als gelesen markieren" button. Verify button immediately changes to "Gelesen am [today's date]". Verify the transition is instant (optimistic UI).

**Version reset (READ-02):** If pageId 1 is configured with version mismatch, verify yellow/orange warning banner appears with "Dieser Artikel wurde aktualisiert. Bitte erneut bestatigen." message. Verify previous read date shown with strikethrough. Verify "Erneut bestatigen" button.

**Pflichtartikel badge:** For mandatory unread articles, verify "Pflichtartikel" warning badge appears above mark-as-read button.

**Flag (SIDE-04):** Click "Als veraltet melden" button. Verify dialog opens with title "Als veraltet melden". Verify reason field is required (Melden button disabled when empty). Type a reason and click "Melden". Verify button changes to "Gemeldet am [date]" (disabled).

**Favorite (SIDE-05):** Verify star icon visible in actions row. Click star -- verify it toggles between outline and filled. Verify toggle is instant (optimistic UI).

**TOC (SIDE-06):** Note: TOC may be hidden entirely in workbench (no CanvasZone with h2/h3 headings). This is expected behavior.

**Version history link (SIDE-07):** Verify "Versionsverlauf anzeigen" link appears. Verify link points to /_layouts/15/Versions.aspx URL pattern.

**Edit metadata button (SIDE-08):** With mockRole set to "reader", verify "Metadaten bearbeiten" button is NOT visible. Change mockRole to "editor" in property pane and verify "Metadaten bearbeiten" button appears.

**Resume signal:** Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean 2>&1 | tail -10</automated>
  </verify>
  <done>User has approved the workbench verification. All 11 phase requirements are verified via build/test success and visual workbench confirmation.</done>
</task>

</tasks>

<verification>
All 11 phase requirements verified via workbench interaction and build/test commands.
</verification>

<success_criteria>
1. All sidebar sections render correctly in workbench
2. Build and tests pass with zero errors
3. User confirms all interactions work as expected
</success_criteria>

<output>
After completion, create `.planning/phases/06-article-sidebar-read-confirmations/06-03-SUMMARY.md`
</output>
