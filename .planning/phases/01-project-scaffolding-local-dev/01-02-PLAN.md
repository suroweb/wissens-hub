---
phase: 01-project-scaffolding-local-dev
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/WissensHub.sln
  - api/src/WissensHub.Functions/WissensHub.Functions.csproj
  - api/src/WissensHub.Functions/Program.cs
  - api/src/WissensHub.Functions/host.json
  - api/src/WissensHub.Functions/local.settings.json
  - api/src/WissensHub.Functions/Functions/HealthFunction.cs
  - api/src/WissensHub.Application/WissensHub.Application.csproj
  - api/src/WissensHub.Domain/WissensHub.Domain.csproj
  - api/src/WissensHub.Domain/Entities/ArticleMetadata.cs
  - api/src/WissensHub.Domain/Entities/ReadConfirmation.cs
  - api/src/WissensHub.Domain/Entities/ArticleFlag.cs
  - api/src/WissensHub.Domain/Entities/Favorite.cs
  - api/src/WissensHub.Domain/Entities/ApprovalHistory.cs
  - api/src/WissensHub.Domain/Entities/Category.cs
  - api/src/WissensHub.Domain/Entities/TargetGroup.cs
  - api/src/WissensHub.Domain/Entities/ArticleTargetGroup.cs
  - api/src/WissensHub.Infrastructure/WissensHub.Infrastructure.csproj
  - api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs
  - api/src/WissensHub.Infrastructure/Data/DesignTimeDbContextFactory.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/ArticleMetadataConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/ReadConfirmationConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/ArticleFlagConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/FavoriteConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/ApprovalHistoryConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/CategoryConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/TargetGroupConfiguration.cs
  - api/src/WissensHub.Infrastructure/Data/Configurations/ArticleTargetGroupConfiguration.cs
  - api/tests/WissensHub.Tests/WissensHub.Tests.csproj
  - api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs
  - docker/docker-compose.yml
  - package.json
  - .gitignore
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Azure Functions project builds and starts locally, /api/health returns 200 with JSON body"
    - "Docker Compose brings up SQL Server 2022 and the container accepts connections on port 1433"
    - "EF Core migrations apply successfully against the Docker SQL container"
    - "Database contains all 8 tables: ArticleMetadata, ReadConfirmations, ArticleFlags, Favorites, ApprovalHistory, Categories, TargetGroups, ArticleTargetGroups"
    - "Root package.json scripts (db:up, db:migrate, api:start, dev) work correctly"
  artifacts:
    - path: "api/src/WissensHub.Functions/Functions/HealthFunction.cs"
      provides: "GET /api/health endpoint"
      contains: "Function(\"Health\")"
    - path: "api/src/WissensHub.Functions/Program.cs"
      provides: "Azure Functions entry point with EF Core DI"
      contains: "FunctionsApplication.CreateBuilder"
    - path: "api/src/WissensHub.Domain/Entities/ArticleMetadata.cs"
      provides: "ArticleMetadata domain entity"
      contains: "class ArticleMetadata"
    - path: "api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs"
      provides: "EF Core DbContext with all 8 DbSets"
      contains: "DbSet<ArticleMetadata>"
    - path: "api/src/WissensHub.Infrastructure/Data/DesignTimeDbContextFactory.cs"
      provides: "Design-time factory for EF Core migration tooling"
      contains: "IDesignTimeDbContextFactory"
    - path: "docker/docker-compose.yml"
      provides: "SQL Server 2022 container for local dev"
      contains: "mcr.microsoft.com/mssql/server:2022-latest"
    - path: "package.json"
      provides: "Root orchestration scripts"
      contains: "concurrently"
    - path: "api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs"
      provides: "Database schema verification tests"
      contains: "DatabaseSchema"
  key_links:
    - from: "api/src/WissensHub.Functions/Program.cs"
      to: "api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs"
      via: "AddDbContext DI registration"
      pattern: "AddDbContext<WissensHubDbContext>"
    - from: "api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs"
      to: "api/src/WissensHub.Domain/Entities/"
      via: "DbSet properties referencing domain entities"
      pattern: "DbSet<.*>"
    - from: "api/src/WissensHub.Infrastructure/Data/Configurations/"
      to: "api/src/WissensHub.Domain/Entities/"
      via: "IEntityTypeConfiguration<T> implementations"
      pattern: "IEntityTypeConfiguration<"
    - from: "docker/docker-compose.yml"
      to: "api/src/WissensHub.Functions/local.settings.json"
      via: "Connection string pointing to localhost:1433"
      pattern: "Server=localhost,1433"
---

<objective>
Create the complete .NET 10 Azure Functions backend with Clean Architecture, all domain entities, EF Core configurations, Docker Compose for local SQL, and root development scripts.

Purpose: Establishes the backend project structure, database schema, and local development workflow that all subsequent API and feature phases depend on.

Output: A buildable .NET solution with 4 projects (Functions, Application, Domain, Infrastructure), 8 EF Core entity configurations, Docker Compose with SQL Server 2022, a health endpoint, database schema tests, and root package.json with dev scripts.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-local-dev/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-local-dev/01-RESEARCH.md
@.planning/phases/01-project-scaffolding-local-dev/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .NET Clean Architecture projects, domain entities, EF Core configs, Docker Compose, and health endpoint</name>
  <files>
    api/WissensHub.sln
    api/src/WissensHub.Functions/WissensHub.Functions.csproj
    api/src/WissensHub.Functions/Program.cs
    api/src/WissensHub.Functions/host.json
    api/src/WissensHub.Functions/local.settings.json
    api/src/WissensHub.Functions/Functions/HealthFunction.cs
    api/src/WissensHub.Application/WissensHub.Application.csproj
    api/src/WissensHub.Domain/WissensHub.Domain.csproj
    api/src/WissensHub.Domain/Entities/ArticleMetadata.cs
    api/src/WissensHub.Domain/Entities/ReadConfirmation.cs
    api/src/WissensHub.Domain/Entities/ArticleFlag.cs
    api/src/WissensHub.Domain/Entities/Favorite.cs
    api/src/WissensHub.Domain/Entities/ApprovalHistory.cs
    api/src/WissensHub.Domain/Entities/Category.cs
    api/src/WissensHub.Domain/Entities/TargetGroup.cs
    api/src/WissensHub.Domain/Entities/ArticleTargetGroup.cs
    api/src/WissensHub.Infrastructure/WissensHub.Infrastructure.csproj
    api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs
    api/src/WissensHub.Infrastructure/Data/DesignTimeDbContextFactory.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/ArticleMetadataConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/ReadConfirmationConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/ArticleFlagConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/FavoriteConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/ApprovalHistoryConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/CategoryConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/TargetGroupConfiguration.cs
    api/src/WissensHub.Infrastructure/Data/Configurations/ArticleTargetGroupConfiguration.cs
    docker/docker-compose.yml
  </files>
  <action>
**Step 1 - Create .NET solution and projects:**

```bash
mkdir -p api/src api/tests
cd api
dotnet new sln -n WissensHub
dotnet new func -n WissensHub.Functions --framework net10.0 --worker-runtime dotnet-isolated -o src/WissensHub.Functions
dotnet new classlib -n WissensHub.Application --framework net10.0 -o src/WissensHub.Application
dotnet new classlib -n WissensHub.Domain --framework net10.0 -o src/WissensHub.Domain
dotnet new classlib -n WissensHub.Infrastructure --framework net10.0 -o src/WissensHub.Infrastructure
dotnet new xunit -n WissensHub.Tests --framework net10.0 -o tests/WissensHub.Tests
```

Add all projects to the solution:
```bash
cd api
dotnet sln add src/WissensHub.Functions src/WissensHub.Application src/WissensHub.Domain src/WissensHub.Infrastructure tests/WissensHub.Tests
```

**Step 2 - Set up project references (Clean Architecture dependency flow):**

Strict reference chain -- Functions -> Application -> Domain; Functions -> Infrastructure -> Domain. Application NEVER references Infrastructure.

```bash
cd api
dotnet add src/WissensHub.Functions reference src/WissensHub.Application src/WissensHub.Infrastructure
dotnet add src/WissensHub.Application reference src/WissensHub.Domain
dotnet add src/WissensHub.Infrastructure reference src/WissensHub.Domain
dotnet add tests/WissensHub.Tests reference src/WissensHub.Functions src/WissensHub.Application src/WissensHub.Domain src/WissensHub.Infrastructure
```

**Step 3 - Add NuGet packages:**

Infrastructure project:
```bash
cd api/src/WissensHub.Infrastructure
dotnet add package Microsoft.EntityFrameworkCore.SqlServer --version 10.0.3
dotnet add package Microsoft.EntityFrameworkCore.Design --version 10.0.3
```

Functions project (EF Core DI):
```bash
cd api/src/WissensHub.Functions
dotnet add package Microsoft.EntityFrameworkCore.SqlServer --version 10.0.3
```

Application project (install MediatR and FluentValidation as placeholders for Phase 4):
```bash
cd api/src/WissensHub.Application
dotnet add package MediatR
dotnet add package FluentValidation
```

Tests project:
```bash
cd api/tests/WissensHub.Tests
dotnet add package Microsoft.EntityFrameworkCore.InMemory --version 10.0.3
```

**Step 4 - Create all 8 domain entities in api/src/WissensHub.Domain/Entities/:**

Remove the auto-generated Class1.cs from Domain, Application, and Infrastructure projects.

Create these entities (follow patterns from 01-RESEARCH.md):

1. **ArticleMetadata.cs** - Id (int, PK), PageId (int, unique), Status (string, default "Draft"), CategoryId (int?, FK), IsMandatory (bool, default false), ReviewById (string?, nvarchar(36) Entra Object ID), ReviewByDate (DateTime?), ContentVersion (int, default 1), CreatedAt (DateTime), UpdatedAt (DateTime). Navigation: Category?, ICollection<ArticleTargetGroup>, ICollection<ReadConfirmation>, ICollection<ArticleFlag>, ICollection<Favorite>, ICollection<ApprovalHistory>.

2. **ReadConfirmation.cs** - Id (int, PK), ArticleMetadataId (int, FK), PageId (int), UserId (string, nvarchar(36)), UserDisplayName (string, nvarchar(256)), ReadDate (DateTime), ContentVersion (int). Navigation: ArticleMetadata?.

3. **ArticleFlag.cs** - Id (int, PK), ArticleMetadataId (int, FK), PageId (int), UserId (string, nvarchar(36)), UserDisplayName (string, nvarchar(256)), Reason (string, nvarchar(1000)), FlaggedDate (DateTime), IsResolved (bool, default false). Navigation: ArticleMetadata?.

4. **Favorite.cs** - Id (int, PK), ArticleMetadataId (int, FK), PageId (int), UserId (string, nvarchar(36)), CreatedAt (DateTime). Navigation: ArticleMetadata?.

5. **ApprovalHistory.cs** - Id (int, PK), ArticleMetadataId (int, FK), PageId (int), Action (string, nvarchar(50) -- "Approved"/"Rejected"/"SubmittedForReview"), ActionBy (string, nvarchar(36) Entra Object ID), ActionByDisplayName (string, nvarchar(256)), ActionDate (DateTime), Comment (string?, nvarchar(2000)). Navigation: ArticleMetadata?.

6. **Category.cs** - Id (int, PK), Name (string, nvarchar(100), unique), Description (string?, nvarchar(500)), IsActive (bool, default true), CreatedAt (DateTime). Navigation: ICollection<ArticleMetadata>.

7. **TargetGroup.cs** - Id (int, PK), Name (string, nvarchar(100), unique), SharePointGroupName (string, nvarchar(256)), IsActive (bool, default true), CreatedAt (DateTime). Navigation: ICollection<ArticleTargetGroup>.

8. **ArticleTargetGroup.cs** - ArticleMetadataId (int, composite PK), TargetGroupId (int, composite PK). Navigation: ArticleMetadata?, TargetGroup?.

**Step 5 - Create WissensHubDbContext:**

File: `api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs`

- 8 DbSet properties (one per entity)
- `OnModelCreating` calls `modelBuilder.ApplyConfigurationsFromAssembly(typeof(WissensHubDbContext).Assembly)`
- Follow the exact pattern from 01-RESEARCH.md

**Step 6 - Create DesignTimeDbContextFactory:**

File: `api/src/WissensHub.Infrastructure/Data/DesignTimeDbContextFactory.cs`

- Implements `IDesignTimeDbContextFactory<WissensHubDbContext>`
- Connection string: `Server=localhost,1433;Database=WissensHub;User Id=sa;Password=WissensHub_Dev2026!;TrustServerCertificate=True;`
- CRITICAL for EF Core migrations to work with Azure Functions isolated worker

**Step 7 - Create all 8 entity configurations in api/src/WissensHub.Infrastructure/Data/Configurations/:**

Each configuration implements `IEntityTypeConfiguration<T>`:

1. **ArticleMetadataConfiguration** - Unique index on PageId, Status required max 50, CategoryId FK with Restrict delete, timestamps
2. **ReadConfirmationConfiguration** - FK to ArticleMetadata with Cascade delete, composite index on (PageId, UserId) for fast lookup, UserId nvarchar(36), UserDisplayName nvarchar(256)
3. **ArticleFlagConfiguration** - FK to ArticleMetadata with Cascade delete, index on PageId, UserId nvarchar(36), Reason nvarchar(1000)
4. **FavoriteConfiguration** - FK to ArticleMetadata with Cascade delete, unique index on (ArticleMetadataId, UserId), UserId nvarchar(36)
5. **ApprovalHistoryConfiguration** - FK to ArticleMetadata with Cascade delete, index on PageId, Action nvarchar(50), ActionBy nvarchar(36), Comment nvarchar(2000) nullable
6. **CategoryConfiguration** - Name required unique nvarchar(100), Description nvarchar(500) nullable
7. **TargetGroupConfiguration** - Name required unique nvarchar(100), SharePointGroupName nvarchar(256)
8. **ArticleTargetGroupConfiguration** - Composite PK (ArticleMetadataId, TargetGroupId), FKs to both entities with Cascade delete

**Step 8 - Create Program.cs:**

File: `api/src/WissensHub.Functions/Program.cs`

- Use `FunctionsApplication.CreateBuilder(args)` pattern (NOT WebApplication)
- Register `WissensHubDbContext` via `AddDbContext` with connection string from configuration
- Follow the exact pattern from 01-RESEARCH.md

**Step 9 - Create HealthFunction.cs:**

File: `api/src/WissensHub.Functions/Functions/HealthFunction.cs`

- Route: GET /api/health
- AuthorizationLevel.Anonymous (local dev, auth comes in Phase 2)
- Returns 200 with JSON: `{"status":"healthy","timestamp":"..."}`
- Follow pattern from 01-RESEARCH.md

**Step 10 - Configure local.settings.json:**

File: `api/src/WissensHub.Functions/local.settings.json`

```json
{
  "IsEncrypted": false,
  "Values": {
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "FUNCTIONS_WORKER_RUNTIME": "dotnet-isolated"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=WissensHub;User Id=sa;Password=WissensHub_Dev2026!;TrustServerCertificate=True;"
  }
}
```

**Step 11 - Configure host.json:**

File: `api/src/WissensHub.Functions/host.json`

```json
{
  "version": "2.0",
  "logging": {
    "applicationInsights": {
      "samplingSettings": {
        "isEnabled": true,
        "excludedTypes": "Request"
      }
    }
  }
}
```

**Step 12 - Create Docker Compose:**

File: `docker/docker-compose.yml`

- Image: `mcr.microsoft.com/mssql/server:2022-latest` (NOT Azure SQL Edge -- retired Sep 2025)
- `platform: linux/amd64` for Rosetta 2 emulation on Apple Silicon
- Container name: `wissenshub-sql`
- SA password: `WissensHub_Dev2026!`
- Port: 1433:1433
- Named volume: `sqldata` (NOT bind mount -- avoids macOS permission issues)
- Follow exact pattern from 01-RESEARCH.md

**Step 13 - Verify .NET solution builds:**
```bash
cd api && dotnet build
```
  </action>
  <verify>
    <automated>cd api && dotnet build</automated>
  </verify>
  <done>
- .NET solution builds with 4 source projects + 1 test project
- Clean Architecture references: Functions -> Application -> Domain, Functions -> Infrastructure -> Domain, Application NEVER references Infrastructure
- All 8 domain entities exist in WissensHub.Domain/Entities/
- WissensHubDbContext has 8 DbSets
- DesignTimeDbContextFactory exists for EF Core tooling
- All 8 entity configurations exist
- HealthFunction responds to GET /api/health
- Docker Compose file uses SQL Server 2022 (not retired SQL Edge)
- local.settings.json has correct connection string
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate EF Core migration, create schema tests, root package.json, and .gitignore</name>
  <files>
    api/src/WissensHub.Infrastructure/Migrations/ (auto-generated)
    api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs
    package.json
    .gitignore
  </files>
  <action>
**Step 1 - Start Docker SQL container:**
```bash
docker compose -f docker/docker-compose.yml up -d
```

Wait for SQL Server to be ready (check with: `docker compose -f docker/docker-compose.yml logs sqlserver` -- look for "SQL Server is now ready for client connections").

**Step 2 - Install dotnet-ef tool globally (if not already installed):**
```bash
dotnet tool install --global dotnet-ef || dotnet tool update --global dotnet-ef
```

**Step 3 - Generate EF Core initial migration:**
```bash
cd api
dotnet ef migrations add InitialCreate --project src/WissensHub.Infrastructure --startup-project src/WissensHub.Functions
```

This creates the migration files in `api/src/WissensHub.Infrastructure/Migrations/`. IMPORTANT: Both `--project` and `--startup-project` flags are required (see Pitfall 7 from research).

**Step 4 - Apply migration to Docker SQL:**
```bash
cd api
dotnet ef database update --project src/WissensHub.Infrastructure --startup-project src/WissensHub.Functions
```

**Step 5 - Create DatabaseSchemaTests:**

File: `api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs`

Create xUnit tests that verify all 8 tables exist in the EF Core model. Use the InMemory provider to validate the model without requiring a live database:

```csharp
using Microsoft.EntityFrameworkCore;
using WissensHub.Infrastructure.Data;

namespace WissensHub.Tests.Infrastructure;

public class DatabaseSchemaTests
{
    private WissensHubDbContext CreateContext()
    {
        var options = new DbContextOptionsBuilder<WissensHubDbContext>()
            .UseInMemoryDatabase(databaseName: Guid.NewGuid().ToString())
            .Options;
        return new WissensHubDbContext(options);
    }

    [Fact]
    public void DbContext_HasAllExpectedDbSets()
    {
        using var context = CreateContext();
        Assert.NotNull(context.ArticleMetadata);
        Assert.NotNull(context.ReadConfirmations);
        Assert.NotNull(context.ArticleFlags);
        Assert.NotNull(context.Favorites);
        Assert.NotNull(context.ApprovalHistory);
        Assert.NotNull(context.Categories);
        Assert.NotNull(context.TargetGroups);
        Assert.NotNull(context.ArticleTargetGroups);
    }

    [Theory]
    [InlineData("ArticleMetadata")]
    [InlineData("ReadConfirmations")]
    [InlineData("ArticleFlags")]
    [InlineData("Favorites")]
    [InlineData("ApprovalHistory")]
    [InlineData("Categories")]
    [InlineData("TargetGroups")]
    [InlineData("ArticleTargetGroups")]
    public void DbContext_HasEntityType(string entityName)
    {
        using var context = CreateContext();
        var entityType = context.Model.FindEntityType($"WissensHub.Domain.Entities.{entityName.TrimEnd('s')}")
            ?? context.Model.FindEntityType($"WissensHub.Domain.Entities.{entityName}");
        Assert.NotNull(entityType);
    }

    [Fact]
    public void ArticleMetadata_HasUniquePageIdIndex()
    {
        using var context = CreateContext();
        var entityType = context.Model.FindEntityType(typeof(WissensHub.Domain.Entities.ArticleMetadata))!;
        var pageIdProperty = entityType.FindProperty("PageId")!;
        var index = entityType.GetIndexes().FirstOrDefault(i => i.Properties.Contains(pageIdProperty));
        Assert.NotNull(index);
        Assert.True(index.IsUnique);
    }

    [Fact]
    public void ArticleTargetGroup_HasCompositePrimaryKey()
    {
        using var context = CreateContext();
        var entityType = context.Model.FindEntityType(typeof(WissensHub.Domain.Entities.ArticleTargetGroup))!;
        var pk = entityType.FindPrimaryKey()!;
        Assert.Equal(2, pk.Properties.Count);
    }
}
```

Adjust entity type names to match actual namespace conventions. The tests must pass with `dotnet test --filter "DatabaseSchema"`.

**Step 6 - Create root package.json:**

File: `package.json` (project root)

```json
{
  "name": "wissens-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n spfx,api -c blue,green \"npm run spfx:serve\" \"npm run api:start\"",
    "spfx:serve": "cd spfx && npm run start",
    "api:start": "cd api/src/WissensHub.Functions && func start",
    "db:up": "docker compose -f docker/docker-compose.yml up -d",
    "db:down": "docker compose -f docker/docker-compose.yml down",
    "db:migrate": "cd api && dotnet ef database update --project src/WissensHub.Infrastructure --startup-project src/WissensHub.Functions"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
```

Then install: `npm install`

**Step 7 - Create .gitignore:**

File: `.gitignore` (project root)

Include standard patterns for:
- Node.js: node_modules/, dist/, lib/, temp/, *.log
- .NET: bin/, obj/, *.user, *.suo, .vs/
- SPFx: sharepoint/solution/*.sppkg, release/
- Docker: (no ignores needed -- compose files are committed)
- IDE: .idea/, .vscode/ (optional -- keep .vscode/settings.json if wanted)
- OS: .DS_Store, Thumbs.db
- Secrets: local.settings.json (IMPORTANT: this contains connection strings), *.pfx
- Specific: !.gitkeep (so empty placeholder dirs are tracked)

**Step 8 - Run all tests:**
```bash
cd api && dotnet test
```

**Step 9 - Stop Docker container:**
```bash
docker compose -f docker/docker-compose.yml down
```
  </action>
  <verify>
    <automated>cd api && dotnet test --filter "DatabaseSchema"</automated>
  </verify>
  <done>
- EF Core InitialCreate migration exists in api/src/WissensHub.Infrastructure/Migrations/
- DatabaseSchemaTests verify all 8 tables/entity types exist and pass
- ArticleMetadata has unique PageId index verified in test
- ArticleTargetGroup has composite PK verified in test
- Root package.json exists with scripts: dev, spfx:serve, api:start, db:up, db:down, db:migrate
- concurrently is installed in root node_modules
- .gitignore covers Node.js, .NET, SPFx, OS, and secrets patterns
- `dotnet test --filter "DatabaseSchema"` passes all tests
  </done>
</task>

</tasks>

<verification>
1. `cd api && dotnet build` exits with code 0
2. `cd api && dotnet test --filter "DatabaseSchema"` -- all tests pass
3. `docker compose -f docker/docker-compose.yml up -d` starts SQL Server container
4. After db:up: `cd api && dotnet ef database update --project src/WissensHub.Infrastructure --startup-project src/WissensHub.Functions` applies migrations
5. `cd api/src/WissensHub.Functions && func start` starts Azure Functions and /api/health returns 200
6. Root `package.json` scripts exist: dev, spfx:serve, api:start, db:up, db:down, db:migrate
7. `.gitignore` exists with appropriate patterns
8. Clean Architecture project references are correct (no circular dependencies)
</verification>

<success_criteria>
- Azure Functions .NET 10 Isolated Worker project builds and serves /api/health (INFRA-03)
- EF Core 10 migrations create all 8 tables successfully (INFRA-04)
- Docker Compose with SQL Server 2022 starts and accepts connections (INFRA-05)
- Clean Architecture: 4 source projects with correct reference chain
- Database schema tests pass verifying all tables
- Root package.json provides developer workflow scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-local-dev/01-02-SUMMARY.md`
</output>
