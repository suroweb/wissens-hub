---
phase: 07-approval-workflow-freigabecenter
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  # Freigabecenter components
  - spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx
  - spfx/src/webparts/freigabecenter/components/Freigabecenter.module.scss
  - spfx/src/webparts/freigabecenter/components/IFreigabecenterProps.ts
  - spfx/src/webparts/freigabecenter/components/PendingTab.tsx
  - spfx/src/webparts/freigabecenter/components/FlaggedTab.tsx
  - spfx/src/webparts/freigabecenter/components/StaleTab.tsx
  - spfx/src/webparts/freigabecenter/components/ApprovalCard.tsx
  - spfx/src/webparts/freigabecenter/components/FlaggedCard.tsx
  - spfx/src/webparts/freigabecenter/components/StaleCard.tsx
  - spfx/src/webparts/freigabecenter/components/ApproveDialog.tsx
  - spfx/src/webparts/freigabecenter/components/RejectDialog.tsx
  # Article Sidebar extensions
  - spfx/src/webparts/articleSidebar/components/ApprovalActions.tsx
  - spfx/src/webparts/articleSidebar/components/ApprovalHistory.tsx
  - spfx/src/webparts/articleSidebar/components/ArticleSidebar.tsx
  - spfx/src/webparts/articleSidebar/components/ArticleSidebar.module.scss
  # Test stubs
  - spfx/src/webparts/freigabecenter/components/__tests__/Freigabecenter.test.tsx
  - spfx/src/webparts/freigabecenter/components/__tests__/PendingTab.test.tsx
  - spfx/src/webparts/freigabecenter/components/__tests__/ApproveDialog.test.tsx
  - spfx/src/webparts/freigabecenter/components/__tests__/RejectDialog.test.tsx
  - spfx/src/webparts/freigabecenter/components/__tests__/FlaggedTab.test.tsx
  - spfx/src/webparts/freigabecenter/components/__tests__/StaleTab.test.tsx
  - spfx/src/webparts/articleSidebar/components/__tests__/ApprovalActions.test.tsx
  - spfx/src/webparts/articleSidebar/components/__tests__/ApprovalHistory.test.tsx
autonomous: true
requirements: [FREI-01, FREI-02, FREI-03, FREI-04, FREI-05, FREI-06, APPR-01, APPR-03]

must_haves:
  truths:
    - "Reviewer can see pending approvals listed as cards with category badge, title, author, submission date, and inline approve/reject buttons"
    - "Reviewer can approve an article via dialog with optional comment -- card is optimistically removed from list"
    - "Reviewer can reject an article via dialog with required comment -- card is optimistically removed from list"
    - "Reviewer can see flagged articles as cards with warning icon, who flagged, reason text"
    - "Reviewer can see stale content alerts with age-colored cards (yellow/orange/red) sorted oldest first"
    - "Reviewer can filter all three tabs by assigned reviewer via dropdown"
    - "Tab badges show correct item counts that update when filter changes or items are removed"
    - "Editor can submit article for review from Article Sidebar when status is Draft"
    - "Reviewer can archive/restore articles from Article Sidebar"
    - "Article Sidebar shows chronological approval history"
  artifacts:
    - path: "spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx"
      provides: "Main container with Pivot tabs and reviewer filter"
      contains: "Pivot"
    - path: "spfx/src/webparts/freigabecenter/components/PendingTab.tsx"
      provides: "Pending approvals tab content"
      contains: "ApprovalCard"
    - path: "spfx/src/webparts/freigabecenter/components/ApproveDialog.tsx"
      provides: "Approve confirmation dialog with optional comment"
      contains: "Genehmigen"
    - path: "spfx/src/webparts/freigabecenter/components/RejectDialog.tsx"
      provides: "Reject dialog with required comment"
      contains: "Ablehnen"
    - path: "spfx/src/webparts/freigabecenter/components/FlaggedTab.tsx"
      provides: "Flagged articles tab content"
      contains: "FlaggedCard"
    - path: "spfx/src/webparts/freigabecenter/components/StaleTab.tsx"
      provides: "Content freshness alerts tab content"
      contains: "StaleCard"
    - path: "spfx/src/webparts/articleSidebar/components/ApprovalActions.tsx"
      provides: "Submit/archive/restore buttons for Article Sidebar"
      contains: "RoleGate"
    - path: "spfx/src/webparts/articleSidebar/components/ApprovalHistory.tsx"
      provides: "Chronological approval history list"
      contains: "useApprovalHistoryQuery"
  key_links:
    - from: "spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx"
      to: "usePendingApprovalsQuery + useFlaggedArticlesQuery + useArticlesQuery"
      via: "hook calls in container"
      pattern: "usePendingApprovalsQuery|useFlaggedArticlesQuery|useArticlesQuery"
    - from: "spfx/src/webparts/freigabecenter/components/ApproveDialog.tsx"
      to: "useApproveArticleCommand"
      via: "command hook"
      pattern: "useApproveArticleCommand"
    - from: "spfx/src/webparts/articleSidebar/components/ApprovalActions.tsx"
      to: "useSubmitForReviewCommand + useArchiveArticleCommand + useRestoreArticleCommand"
      via: "command hooks"
      pattern: "useSubmitForReviewCommand|useArchiveArticleCommand|useRestoreArticleCommand"
    - from: "spfx/src/webparts/articleSidebar/components/ArticleSidebar.tsx"
      to: "ApprovalActions + ApprovalHistory"
      via: "React.createElement composition"
      pattern: "ApprovalActions|ApprovalHistory"
---

<objective>
Freigabecenter Web Part UI with tabbed layout, approval/reject dialogs, and Article Sidebar extensions for status transitions and approval history

Purpose: Build the complete reviewer-facing Freigabecenter with three tabs (pending approvals, flagged articles, stale content) plus extend the Article Sidebar with submit-for-review, archive/restore buttons and approval history display. This makes the approval workflow fully usable from both the central hub and individual article pages.

Output: Working Freigabecenter with Pivot tabs, card layouts, approve/reject dialogs, reviewer filter, and updated Article Sidebar with approval actions and history.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-approval-workflow-freigabecenter/07-CONTEXT.md
@.planning/phases/07-approval-workflow-freigabecenter/07-RESEARCH.md
@.planning/phases/07-approval-workflow-freigabecenter/07-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from Plan 01 that this plan consumes -->

From spfx/src/shared/interfaces/IApprovalService.ts (extended in Plan 01):
```typescript
export interface IApprovalService {
  getPendingApprovals(): Promise<Result<IArticlePage[]>>;
  approveArticle(pageId: number, comment?: string): Promise<Result<void>>;
  rejectArticle(pageId: number, comment: string): Promise<Result<void>>;
  submitForReview(pageId: number): Promise<Result<void>>;
  archiveArticle(pageId: number): Promise<Result<void>>;
  restoreArticle(pageId: number): Promise<Result<void>>;
  getApprovalHistory(pageId: number): Promise<Result<IApprovalAction[]>>;
}
```

From spfx/src/shared/interfaces/IFlagService.ts (extended in Plan 01):
```typescript
export interface IFlagService {
  flagArticle(pageId: number, reason: string): Promise<Result<void>>;
  getFlaggedArticles(): Promise<Result<IFlag[]>>;
}
```

From spfx/src/shared/models/domain/IArticlePage.ts:
```typescript
export interface IArticlePage {
  id: number; title: string; category: string; status: ArticleStatus;
  isMandatory: boolean; targetGroups: string[]; modifiedDate: Date;
  author: IUser; reviewerName?: string; reviewByDate?: Date; url: string;
}
```

From spfx/src/shared/models/domain/IFlag.ts:
```typescript
export interface IFlag {
  id: number; pageId: number; userId: string; userDisplayName: string;
  reason: string; flaggedDate: Date;
}
```

From spfx/src/shared/models/domain/IApprovalAction.ts:
```typescript
export interface IApprovalAction {
  id: number; pageId: number; actionBy: string; actionByDisplayName: string;
  actionDate: Date; fromStatus: ArticleStatus; toStatus: ArticleStatus; comment?: string;
}
```

Available hooks (from Plan 01):
```typescript
usePendingApprovalsQuery(): { state: QueryState<IArticlePage[]>; refetch: () => void }
useFlaggedArticlesQuery(): { state: QueryState<IFlag[]>; refetch: () => void }
useApprovalHistoryQuery(pageId: number): { state: QueryState<IApprovalAction[]>; refetch: () => void }
useApproveArticleCommand(): { state: CommandState; execute: (pageId, comment?) => Promise<boolean> }
useRejectArticleCommand(): { state: CommandState; execute: (pageId, comment) => Promise<boolean> }
useSubmitForReviewCommand(): { state: CommandState; execute: (pageId) => Promise<boolean> }
useArchiveArticleCommand(): { state: CommandState; execute: (pageId) => Promise<boolean> }
useRestoreArticleCommand(): { state: CommandState; execute: (pageId) => Promise<boolean> }
useArticlesQuery(): { state: QueryState<IArticlePage[]>; refetch: () => void }
```

From spfx/src/webparts/dashboard/components/ArticleCard.tsx:
```typescript
// Reference card pattern -- ApprovalCard/FlaggedCard/StaleCard should follow similar structure
export interface IArticleCardProps {
  article: IArticlePage; isUnread: boolean; isFavorite: boolean;
  onFavoriteToggle: (pageId: number) => void; onClick: (url: string) => void;
}
```

From spfx/src/webparts/articleSidebar/components/FlagDialog.tsx:
```typescript
// Reference dialog pattern -- ApproveDialog/RejectDialog should follow this structure
export interface IFlagDialogProps {
  isOpen: boolean; pageId: number; onDismiss: () => void;
  onFlagSubmitted: (flagDate: Date) => void;
}
// Uses: Dialog, DialogType, DialogFooter, TextField, PrimaryButton, DefaultButton
// Pattern: dialogContentProps declared outside component, useCallback for handlers
```

From spfx/src/shared/context (useWissensHub):
```typescript
const { role, currentUser, isLoading, services } = useWissensHub();
```

From spfx/src/shared/models/domain/types.ts:
```typescript
export type ArticleStatus = 'Draft' | 'InReview' | 'Published' | 'Archived';
export type UserRole = 'reader' | 'editor' | 'reviewer' | 'admin';
export const ROLE_HIERARCHY: UserRole[] = ['reader', 'editor', 'reviewer', 'admin'];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freigabecenter core -- Pivot tabs with PendingTab (ApprovalCards + ApproveDialog + RejectDialog), FlaggedTab, StaleTab, reviewer filter, SCSS</name>
  <read_first>
    spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx
    spfx/src/webparts/freigabecenter/components/IFreigabecenterProps.ts
    spfx/src/webparts/freigabecenter/components/Freigabecenter.module.scss
    spfx/src/webparts/freigabecenter/FreigabecenterWebPart.ts
    spfx/src/webparts/dashboard/components/ArticleCard.tsx
    spfx/src/webparts/dashboard/components/Dashboard.module.scss
    spfx/src/webparts/articleSidebar/components/FlagDialog.tsx
    spfx/src/shared/hooks/queries/usePendingApprovalsQuery.ts
    spfx/src/shared/hooks/commands/useApproveArticleCommand.ts
    spfx/src/shared/hooks/commands/useRejectArticleCommand.ts
    spfx/src/shared/models/domain/IArticlePage.ts
    spfx/src/shared/models/domain/IFlag.ts
    spfx/src/shared/models/domain/types.ts
    spfx/src/webparts/dashboard/components/utils/getCategoryColor.ts
    spfx/src/webparts/dashboard/components/utils/formatRelativeDate.ts
  </read_first>
  <files>
    spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx
    spfx/src/webparts/freigabecenter/components/Freigabecenter.module.scss
    spfx/src/webparts/freigabecenter/components/IFreigabecenterProps.ts
    spfx/src/webparts/freigabecenter/components/PendingTab.tsx
    spfx/src/webparts/freigabecenter/components/FlaggedTab.tsx
    spfx/src/webparts/freigabecenter/components/StaleTab.tsx
    spfx/src/webparts/freigabecenter/components/ApprovalCard.tsx
    spfx/src/webparts/freigabecenter/components/FlaggedCard.tsx
    spfx/src/webparts/freigabecenter/components/StaleCard.tsx
    spfx/src/webparts/freigabecenter/components/ApproveDialog.tsx
    spfx/src/webparts/freigabecenter/components/RejectDialog.tsx
  </files>
  <action>
**1. Rewrite `Freigabecenter.tsx`** as the main container component:
- Import hooks: `usePendingApprovalsQuery`, `useFlaggedArticlesQuery`, `useArticlesQuery` from `../../../shared/hooks`
- Import `useApproveArticleCommand`, `useRejectArticleCommand` from `../../../shared/hooks`
- Import `useWissensHub` from `../../../shared/context`
- Import Fluent UI: `Pivot` and `PivotItem` from `@fluentui/react/lib/Pivot`, `Dropdown` and `IDropdownOption` from `@fluentui/react/lib/Dropdown`, `MessageBar` and `MessageBarType` from `@fluentui/react/lib/MessageBar`, `Shimmer` from `@fluentui/react/lib/Shimmer`
- Import child components: PendingTab, FlaggedTab, StaleTab
- State: `selectedReviewer` (string, default 'all'), `approveTarget` (number | undefined for dialog), `rejectTarget` (number | undefined for dialog)
- Data loading:
  - `const pendingQuery = usePendingApprovalsQuery();`
  - `const flaggedQuery = useFlaggedArticlesQuery();`
  - `const articlesQuery = useArticlesQuery();` (for stale articles -- filter Published articles locally)
  - `const approveCommand = useApproveArticleCommand();`
  - `const rejectCommand = useRejectArticleCommand();`
- Derive reviewer dropdown options from pendingQuery.data + flaggedQuery.data + articlesQuery.data: collect unique `reviewerName` values, prepend `{ key: 'all', text: 'Alle Pruefer' }`. Use a `for` loop to collect unique names (no `Set` for ES5).
- Filter data by selectedReviewer:
  - `filteredPending`: if selectedReviewer === 'all', show all; else filter by `a.reviewerName === selectedReviewer`
  - `filteredFlagged`: filter flagged articles whose pageId maps to an article with matching reviewerName (join flags with articles by pageId)
  - `filteredStale`: Published articles where `Math.floor((Date.now() - article.modifiedDate.getTime()) / (1000 * 60 * 60 * 24)) > 90`, filtered by reviewer
  - Sort stale articles oldest first (ascending by modifiedDate)
- Tab counts: `filteredPending.length`, `filteredFlagged.length`, `filteredStale.length`
- Approve handler: `handleApprove(pageId, comment?)` -- optimistically remove from pending list (local state copy), call approve API, on failure refetch
- Reject handler: `handleReject(pageId, comment)` -- same optimistic pattern, call reject API
- Layout:
  ```
  <section className={styles.freigabecenter}>
    <div className={styles.header}>
      <Icon iconName="Approve" className={styles.icon} />
      <h2>Freigabecenter</h2>
    </div>
    <Dropdown label="Pruefer" options={reviewerOptions} selectedKey={selectedReviewer}
      onChange={(_, option) => setSelectedReviewer(option?.key as string || 'all')}
      className={styles.reviewerFilter} />
    <Pivot>
      <PivotItem headerText={'Ausstehend (' + pendingCount + ')'} itemIcon="Clock">
        <PendingTab articles={filteredPending}
          onApprove={(pageId) => setApproveTarget(pageId)}
          onReject={(pageId) => setRejectTarget(pageId)} />
      </PivotItem>
      <PivotItem headerText={'Gemeldet (' + flaggedCount + ')'} itemIcon="Warning">
        <FlaggedTab flags={filteredFlagged} articles={allArticles} />
      </PivotItem>
      <PivotItem headerText={'Veraltet (' + staleCount + ')'} itemIcon="Clock">
        <StaleTab articles={filteredStale} />
      </PivotItem>
    </Pivot>
    <ApproveDialog isOpen={approveTarget !== undefined} pageId={approveTarget || 0}
      articleTitle={getArticleTitle(approveTarget)} onDismiss={() => setApproveTarget(undefined)}
      onApproved={() => { handleApprove(approveTarget!, ...); setApproveTarget(undefined); }} />
    <RejectDialog isOpen={rejectTarget !== undefined} pageId={rejectTarget || 0}
      articleTitle={getArticleTitle(rejectTarget)} onDismiss={() => setRejectTarget(undefined)}
      onRejected={(comment) => { handleReject(rejectTarget!, comment); setRejectTarget(undefined); }} />
  </section>
  ```
- Loading state: Show Shimmer when any query has status 'loading' or 'idle'
- Error state: Show MessageBar with `messageBarType={MessageBarType.error}` and German message "Fehler beim Laden der Daten."
- Use `hasTeamsContext` prop for Teams-specific styling class

**2. Create `ApprovalCard.tsx`:**
Props: `{ article: IArticlePage, onApprove: (pageId: number) => void, onReject: (pageId: number) => void }`
- Card layout following Dashboard ArticleCard pattern but with different action buttons:
  - Category badge (use `getCategoryColor` from dashboard utils -- import via relative path `../../../webparts/dashboard/components/utils/getCategoryColor`)
  - Title (h3)
  - Meta line: `article.author.displayName` + " -- " + `formatRelativeDate(article.modifiedDate)` (use formatRelativeDate from dashboard utils)
  - Reviewer line: "Pruefer: " + (article.reviewerName || "Nicht zugewiesen")
  - Target groups as comma-separated text
  - Action buttons row: PrimaryButton "Genehmigen" with CheckMark icon + DefaultButton "Ablehnen" with Cancel icon
- Use `styles.approvalCard` class

**3. Create `FlaggedCard.tsx`:**
Props: `{ flag: IFlag, articleTitle: string, articleUrl: string }`
- Warning icon (`Warning` from Fluent UI)
- Article title as heading
- "Gemeldet von: " + flag.userDisplayName + " am " + formatDate(flag.flaggedDate)
- Reason text in a blockquote-style area
- "Artikel oeffnen" link (`<a href={articleUrl} target="_blank" rel="noopener noreferrer">`)
- Use `styles.flaggedCard` class

**4. Create `StaleCard.tsx`:**
Props: `{ article: IArticlePage, daysSinceModified: number }`
- Age-colored left border:
  - 90-120 days: `#ffc107` (yellow/amber)
  - 120-180 days: `#ff9800` (orange)
  - 180+ days: `#f44336` (red)
- Set border color via inline style: `style={{ borderLeftColor: ageColor, borderLeftWidth: 4, borderLeftStyle: 'solid' }}`
- Category badge
- Article title
- "Zuletzt geaendert: " + formatDate(article.modifiedDate) + " (" + daysSinceModified + " Tage)"
- "Artikel oeffnen" link
- Use `styles.staleCard` class

**5. Create `PendingTab.tsx`:**
Props: `{ articles: IArticlePage[], onApprove: (pageId: number) => void, onReject: (pageId: number) => void }`
- If articles.length === 0: show empty state "Keine ausstehenden Genehmigungen"
- Else: map articles to ApprovalCard components

**6. Create `FlaggedTab.tsx`:**
Props: `{ flags: IFlag[], articles: IArticlePage[] }`
- If flags.length === 0: show empty state "Keine gemeldeten Artikel"
- Else: for each flag, look up article title and URL from articles array by pageId. Map to FlaggedCard.
- Use `for` loop and manual lookup (no `.find()` for ES5 -- actually `.find()` IS available via TypeScript polyfill in SPFx, but to be safe use a helper function with `for` loop)

**7. Create `StaleTab.tsx`:**
Props: `{ articles: IArticlePage[] }`
- If articles.length === 0: show empty state "Keine veralteten Artikel"
- Else: for each article, compute `daysSinceModified = Math.floor((Date.now() - article.modifiedDate.getTime()) / (1000 * 60 * 60 * 24))`. Map to StaleCard.

**8. Create `ApproveDialog.tsx`:**
Follow exact FlagDialog pattern from Phase 6. Props: `{ isOpen: boolean, pageId: number, articleTitle: string, onDismiss: () => void, onApproved: (comment?: string) => void }`
- dialogContentProps outside component: `{ type: DialogType.normal, title: 'Artikel genehmigen', subText: articleTitle }` -- NOTE: since articleTitle changes, put inside component (differs from FlagDialog where subText was static)
- TextField label="Kommentar" (optional, no `required` prop), multiline, rows=3
- DialogFooter: PrimaryButton "Genehmigen" + DefaultButton "Abbrechen"
- On submit: call onApproved(comment || undefined), reset comment state
- Disable PrimaryButton while parent is executing (pass disabled prop or use internal state)

**9. Create `RejectDialog.tsx`:**
Same structure as ApproveDialog but:
- dialogContentProps title: 'Artikel ablehnen'
- TextField label="Begruendung" with `required` prop
- PrimaryButton "Ablehnen" -- disabled when comment is empty (`!comment.trim()`)
- On submit: call onRejected(comment)

**10. Rewrite `Freigabecenter.module.scss`:**
Keep existing base styles, add:
```scss
.reviewerFilter {
  max-width: 300px;
  margin-bottom: 16px;
}

.approvalCard, .flaggedCard, .staleCard {
  background: "[theme:white, default: #ffffff]";
  background: var(--white, #ffffff);
  border: 1px solid "[theme:neutralLight, default: #edebe9]";
  border: 1px solid var(--neutralLight, #edebe9);
  border-radius: 4px;
  padding: 16px;
  margin-bottom: 12px;
  transition: box-shadow 0.2s;

  &:hover {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }
}

.cardTitle {
  margin: 8px 0 4px;
  font-size: 16px;
  font-weight: 600;
}

.cardMeta {
  font-size: 13px;
  color: "[theme:neutralSecondary, default: #605e5c]";
  color: var(--neutralSecondary, #605e5c);
  margin-bottom: 8px;
}

.categoryBadge {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 2px;
  font-size: 12px;
  font-weight: 600;
  color: #ffffff;
}

.actionsRow {
  display: flex;
  gap: 8px;
  margin-top: 12px;
}

.emptyState {
  padding: 32px 16px;
  text-align: center;
  color: "[theme:neutralSecondary, default: #605e5c]";
  color: var(--neutralSecondary, #605e5c);
  font-size: 14px;
}

.flagReason {
  margin: 8px 0;
  padding: 8px 12px;
  background-color: "[theme:neutralLighter, default: #f3f2f1]";
  background-color: var(--neutralLighter, #f3f2f1);
  border-radius: 4px;
  font-style: italic;
  font-size: 13px;
}

.articleLink {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  margin-top: 8px;
  font-size: 13px;
  color: "[theme:themePrimary, default: #0078d4]";
  color: var(--themePrimary, #0078d4);
  text-decoration: none;
  &:hover { text-decoration: underline; }
}

.warningIcon {
  color: #ff8c00;
  font-size: 18px;
  margin-right: 8px;
}

.tabContent {
  padding: 16px 0;
}

.reviewerInfo {
  font-size: 13px;
  color: "[theme:neutralSecondary, default: #605e5c]";
  color: var(--neutralSecondary, #605e5c);
}

.targetGroups {
  font-size: 12px;
  color: "[theme:neutralTertiary, default: #a19f9d]";
  color: var(--neutralTertiary, #a19f9d);
  margin-top: 4px;
}

.shimmerContainer {
  padding: 16px 0;
}

.shimmerRow {
  margin-bottom: 16px;
}
```

**11. IFreigabecenterProps.ts** -- no changes needed, existing props are sufficient (description, isDarkTheme, environmentMessage, hasTeamsContext, userDisplayName are all available).

**ES5 Compatibility Notes (CRITICAL):**
- Use `indexOf` instead of `includes` everywhere
- Use `for` loops with manual array building instead of `Array.from`, `flatMap`, `Object.values`
- Use `forEach` and `push` instead of `map().filter()` chains where complex
- Date math: `Math.floor((Date.now() - date.getTime()) / (1000 * 60 * 60 * 24))`
- For unique reviewer collection: use a `for` loop checking `indexOf` on accumulated array

**Import paths for shared dashboard utils:**
- `getCategoryColor`: import from `'../../dashboard/components/utils/getCategoryColor'`
- `formatRelativeDate`: import from `'../../dashboard/components/utils/formatRelativeDate'`
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Freigabecenter.tsx contains `Pivot` and `PivotItem` and `Dropdown`
    - Freigabecenter.tsx contains `usePendingApprovalsQuery` and `useFlaggedArticlesQuery` and `useArticlesQuery`
    - Freigabecenter.tsx contains `Ausstehend` and `Gemeldet` and `Veraltet`
    - Freigabecenter.tsx contains `setApproveTarget` and `setRejectTarget`
    - PendingTab.tsx contains `ApprovalCard` and `Keine ausstehenden Genehmigungen`
    - FlaggedTab.tsx contains `FlaggedCard` and `Keine gemeldeten Artikel`
    - StaleTab.tsx contains `StaleCard` and `Keine veralteten Artikel`
    - ApprovalCard.tsx contains `Genehmigen` and `Ablehnen` and `getCategoryColor`
    - FlaggedCard.tsx contains `Warning` and `Gemeldet von` and `Artikel`
    - StaleCard.tsx contains `borderLeftColor` and `Zuletzt` and `Tage`
    - ApproveDialog.tsx contains `Artikel genehmigen` and `Kommentar` and `DialogFooter`
    - RejectDialog.tsx contains `Artikel ablehnen` and `required` and `Begruendung`
    - Freigabecenter.module.scss contains `.approvalCard` and `.flaggedCard` and `.staleCard` and `.reviewerFilter`
    - SPFx `heft build` completes with 0 errors
  </acceptance_criteria>
  <done>Freigabecenter has 3 working Pivot tabs (Ausstehend/Gemeldet/Veraltet) with card layouts, approve/reject dialogs, reviewer filter dropdown, optimistic card removal, and empty states -- all building cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Article Sidebar extensions -- approval action buttons, approval history section, test stubs for both Freigabecenter and Sidebar</name>
  <read_first>
    spfx/src/webparts/articleSidebar/components/ArticleSidebar.tsx
    spfx/src/webparts/articleSidebar/components/ArticleSidebar.module.scss
    spfx/src/webparts/articleSidebar/components/IArticleSidebarProps.ts
    spfx/src/webparts/articleSidebar/components/ReadStatusSection.tsx
    spfx/src/webparts/articleSidebar/components/FlagDialog.tsx
    spfx/src/shared/hooks/queries/useArticleStatusQuery.ts
    spfx/src/shared/hooks/queries/useApprovalHistoryQuery.ts
    spfx/src/shared/hooks/commands/useSubmitForReviewCommand.ts
    spfx/src/shared/hooks/commands/useArchiveArticleCommand.ts
    spfx/src/shared/hooks/commands/useRestoreArticleCommand.ts
    spfx/src/shared/context/index.ts
    spfx/src/webparts/articleSidebar/components/__tests__/ReadStatusSection.test.tsx
    spfx/src/webparts/articleSidebar/components/__tests__/FlagDialog.test.tsx
    spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx
  </read_first>
  <files>
    spfx/src/webparts/articleSidebar/components/ApprovalActions.tsx
    spfx/src/webparts/articleSidebar/components/ApprovalHistory.tsx
    spfx/src/webparts/articleSidebar/components/ArticleSidebar.tsx
    spfx/src/webparts/articleSidebar/components/ArticleSidebar.module.scss
    spfx/src/webparts/freigabecenter/components/__tests__/Freigabecenter.test.tsx
    spfx/src/webparts/freigabecenter/components/__tests__/PendingTab.test.tsx
    spfx/src/webparts/freigabecenter/components/__tests__/ApproveDialog.test.tsx
    spfx/src/webparts/freigabecenter/components/__tests__/RejectDialog.test.tsx
    spfx/src/webparts/freigabecenter/components/__tests__/FlaggedTab.test.tsx
    spfx/src/webparts/freigabecenter/components/__tests__/StaleTab.test.tsx
    spfx/src/webparts/articleSidebar/components/__tests__/ApprovalActions.test.tsx
    spfx/src/webparts/articleSidebar/components/__tests__/ApprovalHistory.test.tsx
  </files>
  <action>
**1. Create `ApprovalActions.tsx`:**
Props: `{ pageId: number, articleStatus: ArticleStatus, onStatusChange: () => void }`
- Import RoleGate from `../../../shared/context`
- Import hooks: `useSubmitForReviewCommand`, `useArchiveArticleCommand`, `useRestoreArticleCommand` from `../../../shared/hooks`
- Import `PrimaryButton`, `DefaultButton` from `@fluentui/react/lib/Button`
- Import `MessageBar`, `MessageBarType` from `@fluentui/react/lib/MessageBar`
- Component logic:
  - Initialize all three command hooks
  - State: `successMessage` (string | undefined) for toast
  - Clear successMessage after 3 seconds via `React.useEffect` with setTimeout
  - `handleSubmitForReview`: call submitForReview.execute(pageId), on success set successMessage to "Zur Pruefung eingereicht", call onStatusChange()
  - `handleArchive`: call archiveArticle.execute(pageId), on success set successMessage to "Artikel archiviert", call onStatusChange()
  - `handleRestore`: call restoreArticle.execute(pageId), on success set successMessage to "Artikel wiederhergestellt", call onStatusChange()
- Render (using React.createElement, consistent with ArticleSidebar pattern):
  - Container div with `styles.approvalActions` class
  - Conditionally show buttons based on articleStatus:
    - `articleStatus === 'Draft'`: RoleGate(minimumRole="editor") wrapping PrimaryButton "Zur Pruefung einreichen" with Send icon, disabled while executing
    - `articleStatus === 'Published'`: RoleGate(minimumRole="reviewer") wrapping DefaultButton "Archivieren" with Archive icon, disabled while executing
    - `articleStatus === 'Archived'`: RoleGate(minimumRole="reviewer") wrapping DefaultButton "Wiederherstellen" with Undo icon, disabled while executing
  - If successMessage: MessageBar with `MessageBarType.success` showing the message

**2. Create `ApprovalHistory.tsx`:**
Props: `{ pageId: number }`
- Import `useApprovalHistoryQuery` from `../../../shared/hooks`
- Import `Icon` from `@fluentui/react/lib/Icon`
- Query approval history: `const historyQuery = useApprovalHistoryQuery(pageId);`
- Render (using React.createElement, matching ArticleSidebar pattern):
  - Section heading: "Genehmigungsverlauf" (h3)
  - Loading: show "Laden..." text
  - Error: show error message
  - Empty: show "Noch keine Aktionen" italic text
  - Success: render chronological list (each item):
    - Action icon: "Accept" for Approved/Restored, "Cancel" for Rejected, "Send" for Submitted, "Archive" for Archived
    - Action text: map action to German label -- "Genehmigt" / "Abgelehnt" / "Eingereicht" / "Archiviert" / "Wiederhergestellt"
    - "von " + actionByDisplayName
    - Date formatted as DD.MM.YYYY (German format)
    - If comment: show comment in smaller italic text
  - Use `styles.approvalHistory`, `styles.historyItem`, `styles.historyAction`, `styles.historyComment` classes

**3. Update `ArticleSidebar.tsx`:**
Add ApprovalActions and ApprovalHistory into the component tree. Import both new components. Insert them between the ReadStatusSection/FlagDialog section and the TableOfContents section:
```typescript
import { ApprovalActions } from './ApprovalActions';
import { ApprovalHistory } from './ApprovalHistory';
```
After the FlagDialog createElement, before the divider + TableOfContents, add:
```typescript
React.createElement('div', { className: styles.divider }),
React.createElement(ApprovalActions, {
  pageId: pageId,
  articleStatus: article ? article.status : 'Draft',
  onStatusChange: () => articleStatus.refetch(),
}),
React.createElement('div', { className: styles.divider }),
React.createElement(ApprovalHistory, { pageId: pageId }),
```

**4. Update `ArticleSidebar.module.scss`:**
Add new styles at the end of the file:
```scss
.approvalActions {
  padding: 8px 0;
}

.approvalHistory {
  padding: 8px 0;
}

.historyItem {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  padding: 6px 0;
  border-bottom: 1px solid var(--neutralLighter, #f3f2f1);
  font-size: 13px;

  &:last-child {
    border-bottom: none;
  }
}

.historyAction {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.historyComment {
  font-style: italic;
  font-size: 12px;
  color: var(--neutralSecondary, #605e5c);
  margin-top: 2px;
}

.historyDate {
  font-size: 12px;
  color: var(--neutralTertiary, #a19f9d);
}
```

**5. Create test stubs** following Phase 5 test stub pattern (describe blocks mapping to requirement IDs, placeholder assertions with TODO comments):

`Freigabecenter.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('Freigabecenter', () => {
  // FREI-01: Pending approvals list
  it('should render Pivot with three tabs', () => {
    expect(true).toBe(true); // TODO: Plan 07 adds real test
  });

  // FREI-06: Filter by reviewer
  it('should render reviewer filter dropdown', () => {
    expect(true).toBe(true); // TODO: Plan 07 adds real test
  });

  it('should filter pending articles by selected reviewer', () => {
    expect(true).toBe(true); // TODO: Plan 07 adds real test
  });

  it('should update tab counts when filter changes', () => {
    expect(true).toBe(true); // TODO: Plan 07 adds real test
  });
});
```

`PendingTab.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('PendingTab', () => {
  // FREI-01
  it('should render approval cards for pending articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show empty state when no pending articles', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`ApproveDialog.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('ApproveDialog', () => {
  // FREI-02
  it('should render dialog with optional comment field', () => {
    expect(true).toBe(true); // TODO
  });

  it('should call onApproved with comment when submitted', () => {
    expect(true).toBe(true); // TODO
  });

  it('should allow submission without comment', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`RejectDialog.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('RejectDialog', () => {
  // FREI-03
  it('should render dialog with required comment field', () => {
    expect(true).toBe(true); // TODO
  });

  it('should disable submit button when comment is empty', () => {
    expect(true).toBe(true); // TODO
  });

  it('should call onRejected with comment when submitted', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`FlaggedTab.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('FlaggedTab', () => {
  // FREI-04
  it('should render flagged article cards', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show empty state when no flagged articles', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`StaleTab.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('StaleTab', () => {
  // FREI-05
  it('should render stale article cards sorted oldest first', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show yellow border for 90-120 day old articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show orange border for 120-180 day old articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show red border for 180+ day old articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show empty state when no stale articles', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`ApprovalActions.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('ApprovalActions', () => {
  // APPR-01: Status transitions
  it('should show submit button for Draft articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show archive button for Published articles', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show restore button for Archived articles', () => {
    expect(true).toBe(true); // TODO
  });

  // APPR-03: Dual-store update
  it('should call onStatusChange after successful action', () => {
    expect(true).toBe(true); // TODO
  });
});
```

`ApprovalHistory.test.tsx`:
```typescript
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from 'react';

describe('ApprovalHistory', () => {
  // APPR-02
  it('should render chronological approval history list', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show action type with German label', () => {
    expect(true).toBe(true); // TODO
  });

  it('should show empty state when no history', () => {
    expect(true).toBe(true); // TODO
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ApprovalActions.tsx contains `useSubmitForReviewCommand` and `useArchiveArticleCommand` and `useRestoreArticleCommand`
    - ApprovalActions.tsx contains `RoleGate` and `minimumRole`
    - ApprovalActions.tsx contains `Zur Pruefung einreichen` and `Archivieren` and `Wiederherstellen`
    - ApprovalHistory.tsx contains `useApprovalHistoryQuery` and `Genehmigungsverlauf`
    - ApprovalHistory.tsx contains `Genehmigt` and `Abgelehnt` and `Eingereicht` and `Archiviert`
    - ArticleSidebar.tsx contains `ApprovalActions` and `ApprovalHistory`
    - ArticleSidebar.module.scss contains `.approvalActions` and `.approvalHistory` and `.historyItem`
    - 8 test stub files exist in __tests__ directories (6 Freigabecenter + 2 Sidebar)
    - `npx heft test --clean` passes (all tests including new stubs)
    - Build exits 0
  </acceptance_criteria>
  <done>Article Sidebar shows role-gated approval action buttons (submit/archive/restore) and chronological approval history. All 8 test stub files created. Full test suite passes including existing 56+ tests and new stubs.</done>
</task>

</tasks>

<verification>
- SPFx builds: `cd spfx && npx heft build --clean` exits 0
- All tests pass: `cd spfx && npx heft test --clean` exits 0 (56+ existing + new stubs)
- Freigabecenter renders in workbench with 3 tabs, reviewer filter, approval cards
- Article Sidebar shows approval actions and history sections
</verification>

<success_criteria>
- Freigabecenter has Pivot with "Ausstehend", "Gemeldet", "Veraltet" tabs with badge counts
- Pending tab shows ApprovalCards with inline approve/reject buttons opening dialogs
- Approve dialog has optional comment, reject dialog has required comment
- Flagged tab shows FlaggedCards with warning icon and flag reason
- Stale tab shows age-colored StaleCards sorted oldest first (yellow 90-120, orange 120-180, red 180+)
- Reviewer filter dropdown filters all three tabs
- Article Sidebar has submit-for-review (editor, Draft), archive (reviewer, Published), restore (reviewer, Archived) buttons
- Article Sidebar shows chronological approval history with German labels
- 8 test stub files created matching VALIDATION.md Wave 0 requirements
- Full SPFx build and test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-approval-workflow-freigabecenter/07-02-SUMMARY.md`
</output>
