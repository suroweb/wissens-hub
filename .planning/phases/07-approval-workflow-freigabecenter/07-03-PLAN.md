---
phase: 07-approval-workflow-freigabecenter
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: []
autonomous: false
requirements: [APPR-01, APPR-02, APPR-03, FREI-01, FREI-02, FREI-03, FREI-04, FREI-05, FREI-06]

must_haves:
  truths:
    - "All 9 phase requirements verified via build, tests, and workbench visual inspection"
    - "Freigabecenter displays correctly with mock data in workbench"
    - "Article Sidebar shows approval actions and history"
  artifacts: []
  key_links: []
---

<objective>
Workbench verification checkpoint for all 9 Phase 7 requirements

Purpose: Verify the complete approval workflow and Freigabecenter implementation against all requirements before marking the phase complete. The reviewer visually inspects the workbench to confirm the UI works correctly with mock data.

Output: Verification confirmation that all APPR-01..03 and FREI-01..06 requirements are satisfied.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-approval-workflow-freigabecenter/07-CONTEXT.md
@.planning/phases/07-approval-workflow-freigabecenter/07-01-SUMMARY.md
@.planning/phases/07-approval-workflow-freigabecenter/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all 9 Phase 7 requirements in workbench</name>
  <read_first>
    .planning/phases/07-approval-workflow-freigabecenter/07-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files></files>
  <action>
Run automated pre-checks (build + test) then present the workbench verification checklist to the user for visual inspection. No code changes in this task -- verification only.

Pre-check commands:
```bash
cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/api && dotnet build --no-restore
cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean && npx heft test --clean
```
  </action>
  <what-built>
Complete approval workflow and Freigabecenter implementation:

**Backend (Plan 01):**
- Real ApproveArticleHandler replacing mock (validates state transitions, updates ArticleMetadata.Status, creates ApprovalHistory)
- GET /api/articles/flagged endpoint (returns unresolved flags)
- GET /api/articles/{pageId}/history endpoint (returns approval history)
- Extended validator accepting 5 action types (Approved/Rejected/Submitted/Archived/Restored)

**Freigabecenter Web Part (Plan 02):**
- Pivot with 3 tabs: "Ausstehend" (pending approvals), "Gemeldet" (flagged articles), "Veraltet" (stale content)
- Tab badges showing item counts
- Reviewer filter dropdown at top, filtering all tabs
- ApprovalCard with inline "Genehmigen"/"Ablehnen" buttons
- ApproveDialog with optional comment, RejectDialog with required comment
- FlaggedCard with warning icon, flag reason, "Artikel oeffnen" link
- StaleCard with age-colored borders (yellow/orange/red), sorted oldest first
- Optimistic card removal after approve/reject

**Article Sidebar Extensions (Plan 02):**
- "Zur Pruefung einreichen" button (editor, when status=Draft)
- "Archivieren" button (reviewer, when status=Published)
- "Wiederherstellen" button (reviewer, when status=Archived)
- Approval history section showing chronological actions list
  </what-built>
  <how-to-verify>
**Pre-check (automated -- run before visual inspection):**
```bash
cd spfx && npx heft build --clean && npx heft test --clean
```
Both must pass with 0 errors.

**Visual inspection in workbench:**

1. Start local server: `cd spfx && npx gulp serve`
2. Open the workbench: `https://{tenant}.sharepoint.com/sites/WissensHub/_layouts/15/workbench.aspx`
3. Set mock role to "reviewer" in property pane

**Freigabecenter Web Part checks:**

4. Add Freigabecenter web part to workbench
5. **FREI-01**: Verify "Ausstehend" tab shows at least 1 pending article card (Datensicherung-Konzept, status=InReview in mock data)
6. **FREI-06**: Verify reviewer filter dropdown appears. Select a specific reviewer name -- verify tab counts update. Select "Alle Pruefer" -- verify all items return.
7. **FREI-02**: Click "Genehmigen" on a pending card -- verify ApproveDialog opens with article title, optional comment field, "Genehmigen" and "Abbrechen" buttons. Close dialog.
8. **FREI-03**: Click "Ablehnen" on a pending card -- verify RejectDialog opens with required comment field, "Ablehnen" button disabled until text entered. Close dialog.
9. **FREI-04**: Click "Gemeldet" tab -- verify flagged article card shows with warning icon, flag reason, and article link
10. **FREI-05**: Click "Veraltet" tab -- verify stale articles show with colored left borders (articles with modifiedDate > 90 days ago)

**Article Sidebar checks:**

11. Add ArticleSidebar web part to workbench (set pageId in property pane)
12. **APPR-01**: Set mock role to "editor" -- verify "Zur Pruefung einreichen" button appears for Draft articles. Set mock role to "reviewer" -- verify "Archivieren" button appears for Published articles.
13. **APPR-02**: Verify approval history section shows "Genehmigungsverlauf" heading with chronological entries
14. **APPR-03**: N/A for workbench (dual-store requires live SharePoint -- confirmed via backend build)
  </how-to-verify>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SPFx build exits 0
    - SPFx test suite passes (all tests including new stubs)
    - Backend build exits 0
    - User confirms visual inspection of all 9 requirements
  </acceptance_criteria>
  <done>All 9 Phase 7 requirements verified via automated build/test and workbench visual inspection. User approved the checkpoint.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Build: `cd spfx && npx heft build --clean` exits 0
- Tests: `cd spfx && npx heft test --clean` exits 0
- Backend: `cd api && dotnet build --no-restore` exits 0
- All 9 requirements (APPR-01..03, FREI-01..06) verified
</verification>

<success_criteria>
- SPFx builds with 0 errors
- All tests pass (existing + new stubs)
- Backend builds with 0 errors
- Workbench visual inspection confirms all 9 requirements
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/07-approval-workflow-freigabecenter/07-03-SUMMARY.md`
</output>
