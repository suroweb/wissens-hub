---
phase: 09-admin-panel-reporting
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified: []
autonomous: false
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, RMND-02]

must_haves:
  truths:
    - "All 7 Phase 9 requirements verified via build, tests, and workbench inspection"
    - "Admin Panel renders correctly in workbench with mock role set to admin"
    - "Non-admin users see empty content"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 9 requirements (ADMIN-01 through ADMIN-06 and RMND-02) via build validation, test execution, and workbench visual inspection.

Purpose: Confirm the Admin Panel meets all acceptance criteria before marking Phase 9 complete.

Output: Verification checkpoint confirming all 7 requirements pass.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-panel-reporting/09-CONTEXT.md
@.planning/phases/09-admin-panel-reporting/09-01-SUMMARY.md
@.planning/phases/09-admin-panel-reporting/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 9 requirements via build, tests, and workbench inspection</name>
  <files></files>
  <read_first>
    .planning/phases/09-admin-panel-reporting/09-01-SUMMARY.md,
    .planning/phases/09-admin-panel-reporting/09-02-SUMMARY.md,
    spfx/src/webparts/adminPanel/components/AdminPanel.tsx,
    spfx/src/webparts/adminPanel/components/KategorienTab.tsx,
    spfx/src/webparts/adminPanel/components/ZielgruppenTab.tsx,
    spfx/src/webparts/adminPanel/components/BerichteTab.tsx,
    spfx/src/webparts/adminPanel/components/ReadReportDrilldown.tsx,
    spfx/src/webparts/adminPanel/components/UebersichtTab.tsx,
    spfx/src/webparts/adminPanel/components/StatsCards.tsx,
    spfx/src/webparts/adminPanel/components/ExportButton.tsx,
    spfx/src/shared/utils/exportUtils.ts,
    spfx/src/shared/utils/freshnessUtils.ts,
    spfx/src/shared/interfaces/IAdminService.ts,
    spfx/src/shared/services/AdminService.ts,
    spfx/src/shared/services/__mocks__/MockAdminService.ts,
    api/src/WissensHub.Functions/Functions/AdminFunctions.cs,
    api/src/WissensHub.Functions/Program.cs
  </read_first>
  <action>
Run build and test suite, then present verification checklist to user for approval.

1. Run `cd api && dotnet build --no-restore` and confirm 0 errors
2. Run `cd spfx && npx heft build --clean` and confirm 0 errors
3. Run `cd spfx && npx heft test --clean` and confirm all tests pass
4. Read source files to verify requirement implementations

**Requirements checklist:**
- **ADMIN-01**: KategorienTab.tsx contains DetailsList, CommandBar with "Hinzufugen", Toggle for IsActive, inline editing with TextField, useSaveCategoryCommand, useDeleteCategoryCommand. Backend has CreateCategory, UpdateCategory, DeleteCategory CQRS commands.
- **ADMIN-02**: ZielgruppenTab.tsx contains Dropdown for SP groups, siteGroups import, useTargetGroupsQuery, useSaveTargetGroupCommand. Backend has CreateTargetGroup, UpdateTargetGroup, DeleteTargetGroup CQRS commands.
- **ADMIN-03**: UebersichtTab.tsx contains Dropdown with interval options (7/14/30/60/90 Tage), useReminderConfigQuery, useUpdateReminderConfigCommand. Backend has SystemConfiguration entity and UpdateReminderInterval command.
- **ADMIN-04**: BerichteTab.tsx contains useAdminReportsQuery, ProgressIndicator for read %, drill-down via selectedPageId. ReadReportDrilldown.tsx contains useDetailedReadStatsQuery, user list with read status icons.
- **ADMIN-05**: exportUtils.ts contains exportToExcel (ExcelJS Workbook) and exportToCsv (with BOM). ExportButton.tsx renders CSV and Excel options. BerichteTab.tsx and ReadReportDrilldown.tsx both use ExportButton.
- **ADMIN-06**: UebersichtTab.tsx contains StatsCards (5 metrics), sortable DetailsList with freshness color coding (getFreshnessLabel, getFreshnessColor), status filter Dropdown.
- **RMND-02**: Same as ADMIN-03 -- the UI stores the interval in SystemConfiguration via API. Actual email sending is out of Phase 9 scope per CONTEXT.md.

**Workbench verification steps:**
1. Start the workbench: `cd spfx && npm start`
2. Navigate to https://localhost:4321/temp/workbench.html
3. Add the Admin Panel web part
4. In the property pane, set Mock Role to "Admin"
5. Verify Ubersicht tab shows stats cards, sortable table with freshness colors, reminder interval config
6. Verify Kategorien tab shows inline-editable category CRUD with DetailsList
7. Verify Zielgruppen tab shows target group management with SharePoint group dropdown
8. Verify Berichte tab shows article list with ProgressIndicator bars, drill-down, and export
9. Change Mock Role to "Reader" and verify Admin Panel shows empty content (RoleGate blocks)
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft build --clean 2>&1 | tail -5 && npx heft test --clean 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && dotnet build --no-restore` exits with code 0
    - `cd spfx && npx heft build --clean` exits with code 0
    - `cd spfx && npx heft test --clean` exits with code 0
    - KategorienTab.tsx contains `DetailsList` and `CommandBar` and `Toggle` and `useSaveCategoryCommand`
    - ZielgruppenTab.tsx contains `siteGroups` and `Dropdown` and `useSaveTargetGroupCommand`
    - UebersichtTab.tsx contains `useReminderConfigQuery` and `useUpdateReminderConfigCommand`
    - BerichteTab.tsx contains `ProgressIndicator` and `useAdminReportsQuery` and `ExportButton`
    - ReadReportDrilldown.tsx contains `useDetailedReadStatsQuery` and `ExportButton`
    - exportUtils.ts contains `Workbook` and `saveAs` and `\uFEFF`
    - AdminPanel.tsx contains `RoleGate` and `Pivot` and `defaultSelectedKey`
    - All 7 Phase 9 requirements confirmed via code inspection and workbench testing
  </acceptance_criteria>
  <done>All 7 Phase 9 requirements verified and approved. Build passes with 0 errors, all tests pass, user confirms all Admin Panel tabs work correctly in workbench.</done>
</task>

</tasks>

<verification>
All 7 Phase 9 requirements (ADMIN-01 through ADMIN-06 and RMND-02) verified via:
1. Backend builds with 0 errors
2. Frontend builds with 0 errors
3. All existing tests pass
4. Source code inspection confirms all requirement implementations
5. Human visual verification in workbench confirms UI functionality
</verification>

<success_criteria>
1. Both api/ and spfx/ build with 0 errors
2. All existing test suites pass (no regressions)
3. All 7 requirement IDs have traceable implementations
4. Human approves workbench visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-panel-reporting/09-03-SUMMARY.md`
</output>
