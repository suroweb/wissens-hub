---
phase: 11-testing
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - spfx/src/shared/test-utils.tsx
  - spfx/package.json
  - api/tests/WissensHub.Tests/WissensHub.Tests.csproj
  - api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs
  - api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs
  - e2e/package.json
  - e2e/tsconfig.json
  - e2e/playwright.config.ts
  - e2e/global-setup.ts
  - e2e/fixtures/spfx-fixtures.ts
  - e2e/.gitignore
  - package.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "renderWithContext() helper renders components with mock services and WissensHubContext"
    - "Backend integration test fixture connects to real SQL Server in Docker and applies migrations"
    - "Playwright E2E scaffold exists with Edge SSO auth and workbench fixtures"
    - "Unified npm scripts run all test suites from root"
  artifacts:
    - path: "spfx/src/shared/test-utils.tsx"
      provides: "renderWithContext() helper with mock services, context, toast"
      contains: "renderWithContext"
    - path: "api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs"
      provides: "xUnit IAsyncLifetime fixture with real SQL Server"
      contains: "IntegrationTestFixture"
    - path: "api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs"
      provides: "Base class with per-test cleanup"
      contains: "IntegrationTestBase"
    - path: "e2e/playwright.config.ts"
      provides: "Playwright config for WissensHub"
      contains: "msedge"
    - path: "e2e/global-setup.ts"
      provides: "Edge SSO auth setup"
      contains: "globalSetup"
    - path: "e2e/fixtures/spfx-fixtures.ts"
      provides: "Worker-scoped workbench fixture"
      contains: "loadWorkbench"
    - path: "package.json"
      provides: "Unified test scripts"
      contains: "test:all"
  key_links:
    - from: "spfx/src/shared/test-utils.tsx"
      to: "spfx/src/shared/services/__mocks__/index.ts"
      via: "createMockServices() import"
      pattern: "createMockServices"
    - from: "api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs"
      to: "api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs"
      via: "EF Core DbContext with SqlServer provider"
      pattern: "UseSqlServer"

user_setup:
  - service: docker-sql-server
    why: "Backend integration tests require SQL Server 2022 running in Docker"
    env_vars: []
    dashboard_config:
      - task: "Start Docker SQL Server container"
        location: "Run: docker-compose -f docker/docker-compose.yml up -d"
---

<objective>
Set up all test infrastructure required by Plans 01-04: frontend renderWithContext() helper, backend integration test fixture with real SQL Server, Playwright E2E scaffold with Edge SSO, and unified npm scripts.

Purpose: Provides the shared test foundation so all subsequent test plans can execute without building infrastructure.
Output: test-utils.tsx, IntegrationTestFixture.cs, IntegrationTestBase.cs, Playwright config/fixtures/auth, root npm scripts.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-testing/11-CONTEXT.md
@.planning/phases/11-testing/11-RESEARCH.md

<interfaces>
<!-- Key types the executor needs for test-utils.tsx -->

From spfx/src/shared/context/WissensHubContext.tsx:
```typescript
export interface IWissensHubContext {
  services: IServiceContainer;
  currentUser: ICurrentUser;
  role: UserRole;
  isLoading: boolean;
}
export const WissensHubContext: React.Context<IWissensHubContext | undefined>;
```

From spfx/src/shared/context/ServiceContainer.ts:
```typescript
export interface IServiceContainer {
  pageService: IPageService;
  apiClient: IApiClient;
  readConfirmationService: IReadConfirmationService;
  favoriteService: IFavoriteService;
  flagService: IFlagService;
  approvalService: IApprovalService;
  adminService: IAdminService;
  cache: CacheService;
  telemetry: ITelemetryService;
}
```

From spfx/src/shared/services/__mocks__/index.ts:
```typescript
export function createMockServices(): IServiceContainer;
```

From spfx/src/shared/services/__mocks__/mockData.ts:
```typescript
export const MOCK_CURRENT_USER: ICurrentUser = {
  displayName: 'Max Mustermann',
  email: 'max.mustermann@contoso.de',
  loginName: 'i:0#.f|membership|max.mustermann@contoso.de',
};
```

From spfx/src/shared/models/domain/types.ts:
```typescript
export type UserRole = 'reader' | 'editor' | 'reviewer' | 'admin';
```

From api/src/WissensHub.Infrastructure/Services/CurrentUser.cs:
```csharp
public class CurrentUser : ICurrentUser {
    public void SetFromClaimsPrincipal(ClaimsPrincipal principal);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend test infrastructure + E2E scaffold + npm scripts</name>
  <files>
    spfx/src/shared/test-utils.tsx
    spfx/package.json
    e2e/package.json
    e2e/tsconfig.json
    e2e/playwright.config.ts
    e2e/global-setup.ts
    e2e/fixtures/spfx-fixtures.ts
    e2e/.gitignore
    package.json
  </files>
  <read_first>
    spfx/src/shared/context/WissensHubContext.tsx
    spfx/src/shared/context/ServiceContainer.ts
    spfx/src/shared/services/__mocks__/index.ts
    spfx/src/shared/services/__mocks__/mockData.ts
    spfx/src/shared/models/domain/types.ts
    spfx/src/shared/components/ToastProvider.tsx
    spfx/src/extensions/unreadBadge/components/__tests__/UnreadBadgeHeader.test.tsx
    spfx/package.json
    package.json
    ../forge-spfx-webpart-project-management-00/playwright.config.ts
    ../forge-spfx-webpart-project-management-00/verification-tests/global-setup.ts
    ../forge-spfx-webpart-project-management-00/verification-tests/spfx-fixtures.ts
  </read_first>
  <action>
1. **Create `spfx/src/shared/test-utils.tsx`:**
   - Import React, render from `@testing-library/react`, WissensHubContext + IWissensHubContext from `../shared/context/WissensHubContext`, createMockServices from `../shared/services/__mocks__`, MOCK_CURRENT_USER from `../shared/services/__mocks__/mockData`, IServiceContainer from `../shared/context/ServiceContainer`, UserRole from `../shared/models/domain/types`, ToastProvider from `../shared/components/ToastProvider`
   - Export interface `RenderOptions { services?: Partial<IServiceContainer>; role?: UserRole; }`
   - Export function `renderWithContext(ui: React.ReactElement, options: RenderOptions = {}): RenderResult` that:
     - Calls `createMockServices()` for base services
     - Merges `options.services` spread over base services, cast as IServiceContainer
     - Builds `contextValue: IWissensHubContext` with services, MOCK_CURRENT_USER, `options.role || 'admin'`, `isLoading: false`
     - Returns `render(React.createElement(WissensHubContext.Provider, { value: contextValue }, React.createElement(ToastProvider, null, ui)))`
   - Use `React.createElement` throughout (not JSX) for consistency with existing test patterns

2. **Install `@testing-library/user-event@13.5.0` in spfx:**
   - Run `cd spfx && npm install --save-dev @testing-library/user-event@13.5.0`
   - Do NOT install v14 (fake-timer conflicts with React 17 + RTL 12)

3. **Create E2E directory and files:**

   **`e2e/package.json`:**
   ```json
   {
     "name": "wissens-hub-e2e",
     "version": "1.0.0",
     "private": true,
     "scripts": {
       "test": "npx playwright test",
       "test:headed": "npx playwright test --headed"
     },
     "devDependencies": {
       "@playwright/test": "1.58.2"
     }
   }
   ```
   Run `cd e2e && npm install && npx playwright install msedge`

   **`e2e/tsconfig.json`:**
   ```json
   {
     "compilerOptions": {
       "target": "ES2020",
       "module": "commonjs",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "forceConsistentCasingInFileNames": true,
       "outDir": "./dist",
       "rootDir": "."
     },
     "include": ["**/*.ts"]
   }
   ```

   **`e2e/playwright.config.ts`:**
   - Replicate the forge-spfx-webpart-project-management-00 pattern exactly
   - `testDir: './tests'`, `globalSetup: './global-setup.ts'`
   - `fullyParallel: false`, `workers: 1`, `timeout: 60_000`, `retries: 0`
   - `forbidOnly: !!process.env.CI`
   - `reporter: [['json', { outputFile: 'test-results.json' }], ['list']]`
   - `use.channel: 'msedge'`, `use.headless: false` (default for SharePoint SSO)
   - `use.ignoreHTTPSErrors: true`
   - `use.navigationTimeout: 30_000`, `use.actionTimeout: 10_000`
   - `use.trace: 'on-first-retry'`, `use.screenshot: 'only-on-failure'`
   - `launchOptions.args`: `['--disable-features=BlockInsecurePrivateNetworkRequests,PrivateNetworkAccessSendPreflights,PrivateNetworkAccessRespectPreflightResults', '--allow-insecure-localhost']`
   - Single project `msedge` with `viewport: { width: 1440, height: 900 }`

   **`e2e/global-setup.ts`:**
   - Replicate forge-spfx-webpart-project-management-00/verification-tests/global-setup.ts exactly
   - Change WORKBENCH_URL tenant from `contoso` to read from `WISSENSHUB_TENANT` env var or default `contoso` (pattern: `https://${tenant}.sharepoint.com/_layouts/15/workbench.aspx?...`)
   - AUTH_STATE_PATH = `path.resolve(__dirname, '.auth-state.json')`
   - Reuse 8-hour auth state validity window
   - Edge persistent profile for SSO with fallback to interactive login
   - Same browser args for private network requests

   **`e2e/fixtures/spfx-fixtures.ts`:**
   - Replicate forge-spfx-webpart-project-management-00/verification-tests/spfx-fixtures.ts pattern
   - Same worker-scoped `sharedContext` and `sharedPage` fixtures
   - `loadWorkbench()` handles "Load debug scripts" / "Debugskripts laden" button (EN/DE)
   - After clicking "+", search for web part by name passed via test context (not hardcoded)
   - Export `createWebPartFixture(webPartName: string)` function that returns a custom test fixture for that web part name
   - Export `resetWorkbenchState()` for beforeEach cleanup (Escape key to close panels/dialogs)
   - Export `loadWorkbench(page, webPartName)` taking web part name as parameter
   - Use same WORKBENCH_URL with env var tenant pattern

   **`e2e/.gitignore`:**
   ```
   node_modules/
   .auth-state.json
   test-results/
   test-results.json
   playwright-report/
   dist/
   ```

4. **Create `e2e/tests/` directory** (empty, Plan 04 populates it):
   - Create `e2e/tests/.gitkeep`

5. **Update root `package.json` scripts:**
   Add these scripts to the existing scripts object (preserve all existing scripts):
   - `"test:frontend": "cd spfx && npx heft test --clean"`
   - `"test:backend": "cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development"`
   - `"test:e2e": "cd e2e && npx playwright test"`
   - `"test:all": "npm run test:frontend && npm run test:backend"`
   Note: test:all does NOT include E2E because E2E requires a running SharePoint tenant + local dev server
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && test -f spfx/src/shared/test-utils.tsx && test -f e2e/playwright.config.ts && test -f e2e/global-setup.ts && test -f e2e/fixtures/spfx-fixtures.ts && test -f e2e/package.json && grep -q "test:frontend" package.json && grep -q "test:backend" package.json && grep -q "test:all" package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - spfx/src/shared/test-utils.tsx contains `export function renderWithContext`
    - spfx/src/shared/test-utils.tsx contains `createMockServices`
    - spfx/src/shared/test-utils.tsx contains `WissensHubContext.Provider`
    - spfx/src/shared/test-utils.tsx contains `ToastProvider`
    - spfx/package.json contains `@testing-library/user-event` at version `13.5.0`
    - e2e/playwright.config.ts contains `channel: 'msedge'`
    - e2e/playwright.config.ts contains `workers: 1`
    - e2e/playwright.config.ts contains `fullyParallel: false`
    - e2e/global-setup.ts contains `AUTH_STATE_PATH`
    - e2e/global-setup.ts contains `launchPersistentContext`
    - e2e/fixtures/spfx-fixtures.ts contains `loadWorkbench`
    - e2e/fixtures/spfx-fixtures.ts contains `resetWorkbenchState`
    - e2e/.gitignore contains `.auth-state.json`
    - package.json contains `"test:frontend": "cd spfx && npx heft test --clean"`
    - package.json contains `"test:backend": "cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development"`
    - package.json contains `"test:all"`
  </acceptance_criteria>
  <done>Frontend test-utils, E2E scaffold (config + auth + fixtures), and root npm test scripts all exist with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Backend integration test fixture + csproj update</name>
  <files>
    api/tests/WissensHub.Tests/WissensHub.Tests.csproj
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs
  </files>
  <read_first>
    api/tests/WissensHub.Tests/WissensHub.Tests.csproj
    api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs
    api/src/WissensHub.Infrastructure/Services/CurrentUser.cs
    api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs
    api/src/WissensHub.Application/Interfaces/ICurrentUser.cs
    api/src/WissensHub.Functions/Program.cs
    docker/docker-compose.yml
  </read_first>
  <action>
1. **Update `WissensHub.Tests.csproj`:**
   - Remove: `<PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" Version="10.0.3" />`
   - The SqlServer provider is already transitively available via the Infrastructure project reference
   - Keep all other package references unchanged (coverlet, xunit, Test.Sdk, xunit.runner)
   - Keep all project references unchanged

2. **Create `IntegrationTestFixture.cs`:**
   ```csharp
   using System.Security.Claims;
   using Microsoft.EntityFrameworkCore;
   using Microsoft.Extensions.DependencyInjection;
   using FluentValidation;
   using MediatR;
   using WissensHub.Application.Commands.MarkAsRead;
   using WissensHub.Application.Common.Behaviors;
   using WissensHub.Application.Interfaces;
   using WissensHub.Infrastructure.Data;
   using WissensHub.Infrastructure.Repositories;
   using WissensHub.Infrastructure.Services;
   ```
   - Class `IntegrationTestFixture : IAsyncLifetime`
   - Property `IServiceProvider ServiceProvider { get; private set; } = null!;`
   - Connection string: `"Server=localhost,1433;Database=WissensHub_Test;User Id=sa;Password=WissensHub_Dev2026!;TrustServerCertificate=true"`
   - NOTE: Uses `WissensHub_Test` database (NOT `WissensHub` dev database) to avoid corrupting dev data
   - `InitializeAsync()`:
     - Build ServiceCollection with:
       - `AddDbContext<WissensHubDbContext>(options => options.UseSqlServer(connectionString))`
       - `AddMediatR(cfg => { cfg.RegisterServicesFromAssembly(typeof(MarkAsReadCommand).Assembly); cfg.AddOpenBehavior(typeof(ExceptionBehavior<,>)); cfg.AddOpenBehavior(typeof(LoggingBehavior<,>)); cfg.AddOpenBehavior(typeof(AuthorizationBehavior<,>)); cfg.AddOpenBehavior(typeof(ValidationBehavior<,>)); })`
       - `AddValidatorsFromAssembly(typeof(MarkAsReadCommand).Assembly)`
       - All 8 repository registrations: `AddScoped<IReadConfirmationRepository, ReadConfirmationRepository>`, `AddScoped<IFavoriteRepository, FavoriteRepository>`, `AddScoped<IFlagRepository, FlagRepository>`, `AddScoped<IApprovalRepository, ApprovalRepository>`, `AddScoped<IArticleMetadataRepository, ArticleMetadataRepository>`, `AddScoped<ICategoryRepository, CategoryRepository>`, `AddScoped<ITargetGroupRepository, TargetGroupRepository>`, `AddScoped<ISystemConfigurationRepository, SystemConfigurationRepository>`
       - `AddScoped<IUnitOfWork>(sp => sp.GetRequiredService<WissensHubDbContext>())`
       - `AddScoped<ICurrentUser>(_ => { var user = new CurrentUser(); user.SetFromClaimsPrincipal(CreateDevPrincipal()); return user; })`
       - `AddLogging()` for ILogger injection in pipeline behaviors
     - Build ServiceProvider
     - Create scope, get WissensHubDbContext, call `await db.Database.EnsureCreatedAsync()`
     - NOTE: Use `EnsureCreatedAsync` instead of `MigrateAsync` because the test database is recreated each run. `EnsureCreated` is faster and avoids migration history table issues.
   - `DisposeAsync()`:
     - Create scope, get WissensHubDbContext, call `await db.Database.EnsureDeletedAsync()` to clean up test database
     - Dispose ServiceProvider if IDisposable
   - Private `CreateDevPrincipal()`:
     - Returns `new ClaimsPrincipal(new ClaimsIdentity(new[] { new Claim("oid", "test-user-oid"), new Claim("name", "Test User"), new Claim("preferred_username", "test@contoso.de"), new Claim("groups", "WissensHub Owners"), new Claim("groups", "WissensHub Reviewers"), new Claim("groups", "WissensHub Editors"), new Claim("groups", "WissensHub Members") }, "TestAuth"))`
     - All 4 groups included for full endpoint coverage (matches dev-mode auth bypass)

3. **Create `IntegrationTestBase.cs`:**
   ```csharp
   using Microsoft.Extensions.DependencyInjection;
   using MediatR;
   using WissensHub.Infrastructure.Data;
   ```
   - Abstract class `IntegrationTestBase : IClassFixture<IntegrationTestFixture>`
   - Protected readonly `IntegrationTestFixture _fixture`
   - Constructor takes `IntegrationTestFixture fixture`
   - Protected method `IServiceScope CreateScope()` => `_fixture.ServiceProvider.CreateScope()`
   - Protected method `IMediator GetMediator(IServiceScope scope)` => `scope.ServiceProvider.GetRequiredService<IMediator>()`
   - Protected method `WissensHubDbContext GetDbContext(IServiceScope scope)` => `scope.ServiceProvider.GetRequiredService<WissensHubDbContext>()`
   - Protected async method `CleanupAsync()`:
     - Creates scope, gets DbContext
     - Removes all data from all tables in correct order (to respect FK constraints):
       1. `db.ArticleTargetGroups.RemoveRange(db.ArticleTargetGroups)`
       2. `db.ApprovalHistory.RemoveRange(db.ApprovalHistory)`
       3. `db.ArticleFlags.RemoveRange(db.ArticleFlags)`
       4. `db.ReadConfirmations.RemoveRange(db.ReadConfirmations)`
       5. `db.Favorites.RemoveRange(db.Favorites)`
       6. `db.ArticleMetadata.RemoveRange(db.ArticleMetadata)`
       7. `db.Categories.RemoveRange(db.Categories)`
       8. `db.TargetGroups.RemoveRange(db.TargetGroups)`
     - `await db.SaveChangesAsync()`
   - Protected method `SeedArticle(WissensHubDbContext db, int pageId, string title, string status)`:
     - Creates and adds ArticleMetadata with given fields plus defaults (Version=1, CreatedAt=DateTime.UtcNow, etc.)
     - Returns the created entity
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/api && dotnet build tests/WissensHub.Tests/WissensHub.Tests.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - WissensHub.Tests.csproj does NOT contain `Microsoft.EntityFrameworkCore.InMemory`
    - IntegrationTestFixture.cs contains `class IntegrationTestFixture : IAsyncLifetime`
    - IntegrationTestFixture.cs contains `Database=WissensHub_Test`
    - IntegrationTestFixture.cs contains `UseSqlServer`
    - IntegrationTestFixture.cs contains `AddMediatR`
    - IntegrationTestFixture.cs contains `CreateDevPrincipal`
    - IntegrationTestFixture.cs contains `WissensHub Owners`
    - IntegrationTestBase.cs contains `abstract class IntegrationTestBase`
    - IntegrationTestBase.cs contains `IClassFixture<IntegrationTestFixture>`
    - IntegrationTestBase.cs contains `CleanupAsync`
    - IntegrationTestBase.cs contains `SeedArticle`
    - `dotnet build tests/WissensHub.Tests` succeeds with 0 errors
  </acceptance_criteria>
  <done>Backend test infrastructure compiles cleanly with real SQL Server fixture, base class with cleanup helpers, InMemory provider removed</done>
</task>

</tasks>

<verification>
- `test -f spfx/src/shared/test-utils.tsx` -- frontend test helper exists
- `test -f api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs` -- backend fixture exists
- `test -f e2e/playwright.config.ts` -- E2E config exists
- `grep -q "test:all" package.json` -- unified scripts exist
- `cd api && dotnet build tests/WissensHub.Tests` -- backend compiles
</verification>

<success_criteria>
All test infrastructure artifacts exist and compile. Frontend test-utils provides renderWithContext(). Backend fixture connects to real SQL Server. E2E scaffold has Edge SSO auth pattern. Root npm scripts unify test execution.
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing/11-00-SUMMARY.md`
</output>
