---
phase: 11-testing
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "All frontend Jest tests pass via heft test --clean"
    - "All backend integration tests pass via dotnet test against real SQL Server"
    - "All E2E test files compile and are ready for execution"
    - "No stub tests remain (no it.todo, no expect(true).toBe(true))"
  artifacts: []
  key_links: []
---

<objective>
Verify all 3 testing requirements are met: Jest frontend tests pass (TEST-01), .NET integration tests pass (TEST-02), and Playwright E2E tests are ready (TEST-03). Run full suites and confirm results with user.

Purpose: Final gate ensuring comprehensive test coverage before Phase 12.
Output: Verified test results confirming all requirements.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-testing/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites and collect results</name>
  <files></files>
  <read_first>
    package.json
    spfx/package.json
    api/tests/WissensHub.Tests/WissensHub.Tests.csproj
  </read_first>
  <action>
Run both automated test suites and collect results:

1. **Frontend tests:**
   - Run: `cd spfx && npx heft test --clean`
   - Capture total test count and pass/fail
   - Verify no stubs remain: `grep -r "it.todo\|expect(true).toBe(true)" spfx/src/ --include="*.test.*"` should return empty

2. **Backend tests:**
   - Ensure Docker SQL Server is running: `docker-compose -f docker/docker-compose.yml up -d`
   - Wait for SQL Server readiness (5 seconds)
   - Run: `cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development --verbosity normal`
   - Capture total test count and pass/fail

3. **E2E tests (compile check only):**
   - Run: `cd e2e && npx tsc --noEmit`
   - Verify all 4 spec files exist and compile
   - NOTE: Actual E2E execution requires user to have SPFx dev server running + SharePoint tenant access. This is verified in the checkpoint task.

4. **Report summary:**
   - Frontend: X/X tests passed
   - Backend: X/X tests passed
   - E2E: 4 spec files compiled, ready for execution
   - Stubs remaining: 0
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && npm run test:frontend 2>&1 | tail -5 && npm run test:backend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:frontend` exits with 0 failures
    - `npm run test:backend` exits with 0 failures
    - `grep -r "it.todo" spfx/src/ --include="*.test.*"` returns no matches
    - `grep -r "expect(true).toBe(true)" spfx/src/ --include="*.test.*"` returns no matches
    - Frontend test count is at least 80
    - Backend test count is at least 25
    - All 4 E2E spec files exist in e2e/tests/
  </acceptance_criteria>
  <done>All automated test suites pass with zero failures; no test stubs remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of complete test suite</name>
  <files></files>
  <action>
Present test results to user for final verification:
- Frontend: Jest test count and pass/fail summary
- Backend: .NET integration test count and pass/fail summary
- E2E: 4 spec files compiled and ready for execution
- Stub check: zero remaining it.todo or expect(true).toBe(true) stubs

Requirements verified:
- TEST-01: Jest unit tests cover frontend services, hooks, and key components
- TEST-02: .NET integration tests verify all API endpoints against SQL Server 2022 in Docker
- TEST-03: Playwright E2E tests execute critical user flows

User verification steps:
1. Review frontend test output: `cd spfx && npx heft test --clean` -- Expected: 80+ tests passing, 0 failures
2. Review backend test output: `cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development` -- Expected: 25+ tests passing, 0 failures
3. (Optional) Run E2E tests against workbench: Start SPFx dev server `cd spfx && npm start`, then `cd e2e && npx playwright test --headed`
4. Verify no stubs: `grep -r "it.todo\|expect(true).toBe(true)" spfx/src/ --include="*.test.*"` -- Expected: no output
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has reviewed and approved the complete test suite covering TEST-01, TEST-02, and TEST-03</done>
</task>

</tasks>

<verification>
- TEST-01: `cd spfx && npx heft test --clean` passes with 80+ tests
- TEST-02: `cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development` passes with 25+ tests
- TEST-03: 4 E2E spec files exist and compile; ready for execution
- No stubs remain in any test file
</verification>

<success_criteria>
All three testing requirements (TEST-01, TEST-02, TEST-03) are verified passing. User has approved the test results. Phase 11 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing/11-05-SUMMARY.md`
</output>
