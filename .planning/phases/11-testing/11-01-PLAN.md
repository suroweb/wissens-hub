---
phase: 11-testing
plan: 01
type: execute
wave: 2
depends_on: [11-00]
files_modified:
  - spfx/src/shared/services/__tests__/CacheService.test.ts
  - spfx/src/shared/services/__tests__/TelemetryService.test.ts
  - spfx/src/shared/utils/__tests__/exportUtils.test.ts
  - spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx
  - spfx/src/shared/components/__tests__/ToastProvider.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx
  - spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx
  - spfx/src/extensions/unreadBadge/components/__tests__/UnreadFlyoutPanel.test.tsx
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "CacheService tests verify get/set/invalidate/clear/TTL expiry behavior"
    - "TelemetryService tests verify event tracking and exception tracking"
    - "ErrorBoundary test verifies fallback rendering on child error"
    - "ToastProvider test verifies toast display and auto-dismiss"
    - "Dashboard test verifies article rendering with mock data"
    - "FilterBar test verifies filter dropdowns and search input"
    - "StatsBar test verifies stat counters display"
    - "ArticleCard test verifies card content rendering"
  artifacts:
    - path: "spfx/src/shared/services/__tests__/CacheService.test.ts"
      provides: "CacheService unit tests replacing it.todo stubs"
      min_lines: 30
    - path: "spfx/src/shared/services/__tests__/TelemetryService.test.ts"
      provides: "TelemetryService unit tests"
      min_lines: 20
    - path: "spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx"
      provides: "ErrorBoundary component tests"
      min_lines: 25
    - path: "spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx"
      provides: "Dashboard component tests with renderWithContext"
      min_lines: 30
  key_links:
    - from: "spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx"
      to: "spfx/src/shared/test-utils.tsx"
      via: "renderWithContext import"
      pattern: "renderWithContext"
    - from: "spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx"
      to: "spfx/src/shared/components/ErrorBoundary.tsx"
      via: "component import and error simulation"
      pattern: "ErrorBoundary"
---

<objective>
Replace all stub test files for shared services, shared components, Dashboard web part, and UnreadBadge extension with real assertions. This covers 10 test files (8 stubs to replace + 2 already-real UnreadBadge tests to verify still pass).

Purpose: Establishes test coverage for the foundation layer (services, utilities, shared components) and the primary user-facing web part (Dashboard).
Output: 10 test files with real assertions, all passing via `heft test`.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-testing/11-CONTEXT.md
@.planning/phases/11-testing/11-RESEARCH.md
@.planning/phases/11-testing/11-00-SUMMARY.md

<interfaces>
<!-- Virtual module names that must be jest.mock'd with { virtual: true } -->
Dashboard tests need: 'DashboardWebPartStrings', 'SharedStrings'
UnreadBadge tests need: 'UnreadBadgeApplicationCustomizerStrings', 'SharedStrings'

<!-- renderWithContext from Plan 00 -->
From spfx/src/shared/test-utils.tsx:
```typescript
export function renderWithContext(ui: React.ReactElement, options?: { services?: Partial<IServiceContainer>; role?: UserRole }): RenderResult;
```

<!-- Mock services available as test doubles -->
From spfx/src/shared/services/__mocks__/index.ts:
```typescript
export function createMockServices(): IServiceContainer;
```

<!-- CRITICAL: jest.mock must come BEFORE imports per @rushstack/hoist-jest-mock rule -->
<!-- CRITICAL: Use React.createElement (not JSX) for consistency -->
<!-- CRITICAL: ES5 target -- use indexOf/forEach, NOT includes/flatMap -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared services and components tests (7 files)</name>
  <files>
    spfx/src/shared/services/__tests__/CacheService.test.ts
    spfx/src/shared/services/__tests__/TelemetryService.test.ts
    spfx/src/shared/utils/__tests__/exportUtils.test.ts
    spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx
    spfx/src/shared/components/__tests__/ToastProvider.test.tsx
  </files>
  <read_first>
    spfx/src/shared/services/__tests__/CacheService.test.ts
    spfx/src/shared/services/__tests__/TelemetryService.test.ts
    spfx/src/shared/utils/__tests__/exportUtils.test.ts
    spfx/src/shared/components/__tests__/ErrorBoundary.test.tsx
    spfx/src/shared/components/__tests__/ToastProvider.test.tsx
    spfx/src/shared/services/CacheService.ts
    spfx/src/shared/services/TelemetryService.ts
    spfx/src/shared/utils/exportUtils.ts
    spfx/src/shared/components/ErrorBoundary.tsx
    spfx/src/shared/components/ToastProvider.tsx
    spfx/src/shared/test-utils.tsx
  </read_first>
  <action>
Replace all 5 stub files with real test implementations. Each file must follow:
- `jest.mock()` calls BEFORE imports (rushstack/hoist-jest-mock rule)
- Mock `'SharedStrings'` with `{ virtual: true }` when component uses shared loc strings
- Use `React.createElement` not JSX
- Use `indexOf`/`forEach` instead of `includes`/`flatMap` (ES5 target)
- Import `@testing-library/jest-dom` for DOM matchers

**CacheService.test.ts** (replace the 6 it.todo stubs):
- `beforeEach`: create new CacheService instance, call `jest.useFakeTimers()`
- `afterEach`: call `jest.useRealTimers()`
- Test: `get()` returns `undefined` for cache miss
- Test: `set()` + `get()` returns stored value within TTL
- Test: `get()` returns `undefined` after TTL expires (advance timers with `jest.advanceTimersByTime`)
- Test: `invalidate(prefix)` removes all entries matching prefix, preserves others
- Test: `clear()` removes all entries
- Test: `set()` with no TTL uses default TTL (read the default from CacheService source)

**TelemetryService.test.ts** (replace it.todo stubs):
- Test ConsoleTelemetryService (the local dev implementation):
  - `trackEvent(name, properties)` calls `console.log` with event name (spy on console.log)
  - `trackException(error, properties)` calls `console.error` (spy on console.error)
  - `trackMetric(name, value)` calls `console.log` with metric name
- Test that `createTelemetryService('')` returns ConsoleTelemetryService (empty connection string = local mode)

**exportUtils.test.ts** (replace it.todo stubs):
- Read the actual exportUtils.ts to understand what functions are exported
- Test CSV generation: provide array of objects, verify output contains comma-separated headers and values
- Test Excel generation if present (or skip if export is CSV-only)
- Test empty data produces header-only output

**ErrorBoundary.test.tsx** (replace it.todo stubs):
- Mock `'SharedStrings'` with `{ virtual: true }` containing `ErrorOccurred` and `Reload` keys
- Create a `ThrowingComponent` that throws an error when rendered
- Test: renders children normally when no error occurs
- Test: renders fallback UI (containing the error message or reload button) when child throws
- Test: calls `telemetry.trackException` when error occurs (provide mock telemetry via renderWithContext services override: `{ telemetry: { trackEvent: jest.fn(), trackException: mockFn, trackMetric: jest.fn() } }`)
- Suppress console.error during error boundary tests with `jest.spyOn(console, 'error').mockImplementation()`

**ToastProvider.test.tsx** (replace it.todo stubs):
- Read ToastProvider.tsx to understand the toast context API (useToast hook, showToast method, etc.)
- Test: renders children without toasts initially
- Test: shows toast message when `showToast()` is called (trigger via a test component that calls the hook)
- Test: toast auto-dismisses after timeout (use fake timers)
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CacheService.test.ts does NOT contain `it.todo`
    - CacheService.test.ts contains `jest.useFakeTimers`
    - CacheService.test.ts contains `jest.advanceTimersByTime`
    - CacheService.test.ts contains at least 5 `it(` or `test(` blocks
    - TelemetryService.test.ts does NOT contain `it.todo`
    - TelemetryService.test.ts contains `console.log` or `jest.spyOn`
    - exportUtils.test.ts does NOT contain `it.todo`
    - ErrorBoundary.test.tsx does NOT contain `it.todo`
    - ErrorBoundary.test.tsx contains `trackException`
    - ToastProvider.test.tsx does NOT contain `it.todo`
    - All test files pass via `heft test --clean`
  </acceptance_criteria>
  <done>All 5 shared test files have real assertions with no it.todo stubs remaining; all tests pass in Heft pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard web part tests + UnreadBadge verification (5 files)</name>
  <files>
    spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx
    spfx/src/extensions/unreadBadge/components/__tests__/UnreadFlyoutPanel.test.tsx
  </files>
  <read_first>
    spfx/src/webparts/dashboard/components/__tests__/Dashboard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/ArticleCard.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/FilterBar.test.tsx
    spfx/src/webparts/dashboard/components/__tests__/StatsBar.test.tsx
    spfx/src/extensions/unreadBadge/components/__tests__/UnreadFlyoutPanel.test.tsx
    spfx/src/webparts/dashboard/components/Dashboard.tsx
    spfx/src/webparts/dashboard/components/ArticleCard.tsx
    spfx/src/webparts/dashboard/components/FilterBar.tsx
    spfx/src/webparts/dashboard/components/StatsBar.tsx
    spfx/src/extensions/unreadBadge/components/UnreadFlyoutPanel.tsx
    spfx/src/webparts/dashboard/loc/mystrings.d.ts
    spfx/src/shared/loc/mystrings.d.ts
    spfx/src/shared/test-utils.tsx
    spfx/src/shared/services/__mocks__/mockData.ts
    spfx/src/extensions/unreadBadge/components/__tests__/UnreadBadgeHeader.test.tsx
  </read_first>
  <action>
Replace 4 Dashboard stubs with real tests, verify UnreadFlyoutPanel stub is replaced with real tests.

**ALL Dashboard test files must:**
- `jest.mock('DashboardWebPartStrings', () => ({ ... }), { virtual: true })` BEFORE imports
- `jest.mock('SharedStrings', () => ({ ... }), { virtual: true })` BEFORE imports
- Include ALL string keys used by the component (read the component source to identify which strings it uses)
- Use `renderWithContext()` from `../../../../shared/test-utils`
- Use `React.createElement` for component rendering
- Import `@testing-library/jest-dom` after mocks

**Dashboard.test.tsx** (replace 5 `expect(true).toBe(true)` stubs):
- Read Dashboard.tsx to understand its rendering: it uses useWissensHub() for services, loads articles via hooks
- Test: renders without crashing (renderWithContext + component)
- Test: shows shimmer/loading state initially (look for Shimmer component or data-automationid)
- Test: renders article cards after data loads (MockPageService returns MOCK_ARTICLES -- 6 are Published)
- Test: toggles between card and list view when toggle button is clicked (use fireEvent.click)
- Test: shows empty state when no articles match filters (override pageService.getPublishedArticles to return empty)
- NOTE: Dashboard renders asynchronously via useEffect. Use `await screen.findByText(...)` or `waitFor()` for async assertions

**ArticleCard.test.tsx** (replace stubs):
- Read ArticleCard.tsx props interface to understand what props it accepts
- Test: renders article title
- Test: renders article category
- Test: renders author name
- Test: shows mandatory badge when `isMandatory: true`
- Test: shows unread indicator when article is unread
- Pass mock article data as props (use MOCK_ARTICLES[0] pattern)

**FilterBar.test.tsx** (replace stubs):
- Read FilterBar.tsx to understand props: likely filter state + onChange callbacks
- Test: renders category dropdown
- Test: renders search input
- Test: renders view toggle buttons
- Test: calls onFilterChange when category is selected (use fireEvent)
- Test: calls onSearchChange when text is typed in search box

**StatsBar.test.tsx** (replace stubs):
- Read StatsBar.tsx to understand what stats it shows
- Test: renders unread count
- Test: renders favorites count
- Test: renders pending review count (role-gated for reviewer/admin via RoleGate)
- Test: hides pending reviews for reader role (renderWithContext with `role: 'reader'`)

**UnreadFlyoutPanel.test.tsx** (if still a stub, replace):
- Read UnreadFlyoutPanel.tsx and the existing UnreadBadgeHeader.test.tsx for the mocking pattern
- `jest.mock('UnreadBadgeApplicationCustomizerStrings', ...)` and `jest.mock('SharedStrings', ...)` BEFORE imports
- Test: renders article list when isOpen=true
- Test: renders nothing or hidden when isOpen=false
- Test: clicking article triggers navigation (check window.open or link href)
- Test: shows "all read" message when articles array is empty
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/spfx && npx heft test --clean 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.test.tsx does NOT contain `expect(true).toBe(true)`
    - Dashboard.test.tsx contains `renderWithContext`
    - Dashboard.test.tsx contains `jest.mock('DashboardWebPartStrings'`
    - ArticleCard.test.tsx does NOT contain `it.todo` or `expect(true).toBe(true)`
    - FilterBar.test.tsx does NOT contain `it.todo` or `expect(true).toBe(true)`
    - StatsBar.test.tsx does NOT contain `it.todo` or `expect(true).toBe(true)`
    - UnreadFlyoutPanel.test.tsx does NOT contain `it.todo` or `expect(true).toBe(true)`
    - All test files pass via `heft test --clean`
    - Total passing test count is at least 30 (existing UnreadBadgeHeader 6 + new tests)
  </acceptance_criteria>
  <done>All 5 test files have real assertions; Dashboard, ArticleCard, FilterBar, StatsBar, and UnreadFlyoutPanel all test rendering and user interaction; all pass in Heft pipeline</done>
</task>

</tasks>

<verification>
- `cd spfx && npx heft test --clean` passes with 0 failures
- No `it.todo` or `expect(true).toBe(true)` remains in any of the 10 files
- Test count is at least 30 passing tests
</verification>

<success_criteria>
10 test files (5 shared + 4 dashboard + 1 unread badge) all contain real assertions, use renderWithContext where appropriate, mock loc strings with { virtual: true }, and pass in the Heft pipeline. No stubs remain.
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing/11-01-SUMMARY.md`
</output>
