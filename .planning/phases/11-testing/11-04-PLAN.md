---
phase: 11-testing
plan: 04
type: execute
wave: 2
depends_on: [11-00]
files_modified:
  - api/tests/WissensHub.Tests/Infrastructure/DatabaseSchemaTests.cs
  - api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs
  - api/tests/WissensHub.Tests/Endpoints/FavoriteEndpointTests.cs
  - api/tests/WissensHub.Tests/Endpoints/ApprovalEndpointTests.cs
  - api/tests/WissensHub.Tests/Endpoints/DashboardEndpointTests.cs
  - api/tests/WissensHub.Tests/Endpoints/ReadStatsEndpointTests.cs
  - api/tests/WissensHub.Tests/Endpoints/AdminEndpointTests.cs
  - api/tests/WissensHub.Tests/UnitTest1.cs
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Integration tests exercise the full MediatR pipeline (behaviors + handlers + EF Core + SQL Server)"
    - "All 10 API endpoints have at least one happy-path test and one error case test"
    - "Tests run against real SQL Server 2022 in Docker, not EF Core InMemory"
    - "Synthetic dev identity provides all 4 WissensHub groups for full endpoint access"
  artifacts:
    - path: "api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs"
      provides: "Tests for article status, unread, read, flag endpoints"
      contains: "ArticleEndpointTests"
    - path: "api/tests/WissensHub.Tests/Endpoints/FavoriteEndpointTests.cs"
      provides: "Tests for favorites and toggle"
      contains: "FavoriteEndpointTests"
    - path: "api/tests/WissensHub.Tests/Endpoints/ApprovalEndpointTests.cs"
      provides: "Tests for approve/reject"
      contains: "ApprovalEndpointTests"
    - path: "api/tests/WissensHub.Tests/Endpoints/DashboardEndpointTests.cs"
      provides: "Tests for dashboard stats"
      contains: "DashboardEndpointTests"
    - path: "api/tests/WissensHub.Tests/Endpoints/AdminEndpointTests.cs"
      provides: "Tests for admin reports, categories, target groups"
      contains: "AdminEndpointTests"
  key_links:
    - from: "api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs"
      to: "api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs"
      via: "IClassFixture inheritance"
      pattern: "IntegrationTestBase"
    - from: "api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs"
      to: "api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs"
      via: "MediatR.Send(new MarkAsReadCommand(...))"
      pattern: "MarkAsReadCommand"

user_setup:
  - service: docker-sql-server
    why: "Integration tests require SQL Server 2022 running in Docker on port 1433"
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker SQL Server is running"
        location: "Run: docker-compose -f docker/docker-compose.yml up -d"
---

<objective>
Create .NET integration tests for all 10 Azure Functions API endpoints using MediatR handler testing against real SQL Server in Docker. Each endpoint gets at least one happy-path and one error-case test.

Purpose: Validates the full backend pipeline (MediatR behaviors -> handlers -> repositories -> EF Core -> SQL Server) against real database constraints.
Output: 6 endpoint test files + cleanup of UnitTest1.cs, all passing via `dotnet test`.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-testing/11-CONTEXT.md
@.planning/phases/11-testing/11-RESEARCH.md
@.planning/phases/11-testing/11-00-SUMMARY.md

<interfaces>
<!-- IntegrationTestBase from Plan 00 -->
From api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs:
```csharp
public abstract class IntegrationTestBase : IClassFixture<IntegrationTestFixture> {
    protected IServiceScope CreateScope();
    protected IMediator GetMediator(IServiceScope scope);
    protected WissensHubDbContext GetDbContext(IServiceScope scope);
    protected Task CleanupAsync();
    protected ArticleMetadata SeedArticle(WissensHubDbContext db, int pageId, string title, string status);
}
```

<!-- API endpoints mapped to MediatR commands/queries -->
API-01: GET /articles/{pageId}/status -> GetArticleStatusQuery
API-02: GET /articles/unread -> GetUnreadArticlesQuery
API-03: POST /articles/{pageId}/read -> MarkAsReadCommand
API-04: POST /articles/{pageId}/flag -> FlagArticleCommand
API-05: GET /articles/{pageId}/readstats -> GetReadStatsQuery (or GetDetailedReadStatsQuery)
API-06: POST /articles/{pageId}/approve -> ApproveArticleCommand
API-07: GET /favorites -> GetFavoritesQuery
API-08: POST /favorites/{pageId} -> ToggleFavoriteCommand
API-09: GET /dashboard/stats -> GetDashboardStatsQuery
API-10: GET /admin/reports -> GetAdminReportsQuery
Plus: categories CRUD, target groups CRUD, reminder interval config

<!-- CRITICAL: WebApplicationFactory does NOT work with Azure Functions Isolated Worker -->
<!-- Tests send MediatR requests directly via IMediator.Send(), not HTTP -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Article, Favorite, and Approval endpoint tests</name>
  <files>
    api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs
    api/tests/WissensHub.Tests/Endpoints/FavoriteEndpointTests.cs
    api/tests/WissensHub.Tests/Endpoints/ApprovalEndpointTests.cs
    api/tests/WissensHub.Tests/UnitTest1.cs
  </files>
  <read_first>
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs
    api/tests/WissensHub.Tests/UnitTest1.cs
    api/src/WissensHub.Application/Commands/MarkAsRead/MarkAsReadCommand.cs
    api/src/WissensHub.Application/Commands/FlagArticle/FlagArticleCommand.cs
    api/src/WissensHub.Application/Commands/ToggleFavorite/ToggleFavoriteCommand.cs
    api/src/WissensHub.Application/Commands/ApproveArticle/ApproveArticleCommand.cs
    api/src/WissensHub.Application/Queries/GetArticleStatus/GetArticleStatusQuery.cs
    api/src/WissensHub.Application/Queries/GetUnreadArticles/GetUnreadArticlesQuery.cs
    api/src/WissensHub.Application/Queries/GetFavorites/GetFavoritesQuery.cs
    api/src/WissensHub.Application/Queries/GetReadStats/GetReadStatsQuery.cs
    api/src/WissensHub.Application/Queries/GetApprovalHistory/GetApprovalHistoryQuery.cs
    api/src/WissensHub.Domain/Entities/ArticleMetadata.cs
    api/src/WissensHub.Domain/Entities/ReadConfirmation.cs
    api/src/WissensHub.Domain/Entities/Favorite.cs
    api/src/WissensHub.Domain/Entities/ArticleFlag.cs
    api/src/WissensHub.Domain/Entities/ApprovalHistory.cs
    api/src/WissensHub.Infrastructure/Data/WissensHubDbContext.cs
  </read_first>
  <action>
1. **Delete `UnitTest1.cs`** -- it's the default xUnit template file with no real tests.

2. **Create `Endpoints/` directory** under `api/tests/WissensHub.Tests/`.

3. **Create `ArticleEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`, namespace `WissensHub.Tests.Endpoints`
   - Using: `MediatR`, `Microsoft.Extensions.DependencyInjection`, `WissensHub.Application.Commands.*`, `WissensHub.Application.Queries.*`, `WissensHub.Domain.Entities`, `WissensHub.Infrastructure.Data`
   - Constructor calls `base(fixture)` and `CleanupAsync().GetAwaiter().GetResult()` for per-test cleanup

   Tests for **API-01 GetArticleStatus** (GET /articles/{pageId}/status):
   - Happy path: Seed an article, send `GetArticleStatusQuery { PageId = 100 }`, assert response contains title and status
   - Error case: Send query with non-existent PageId, assert result indicates not found or null

   Tests for **API-02 GetUnreadArticles** (GET /articles/unread):
   - Happy path: Seed 2 articles (Published), send `GetUnreadArticlesQuery`, assert result contains 2 unread articles
   - After marking one as read: seed + mark as read + query again, assert only 1 unread

   Tests for **API-03 MarkAsRead** (POST /articles/{pageId}/read):
   - Happy path: Seed article, send `MarkAsReadCommand { PageId = 100 }`, assert success; verify ReadConfirmation row in DB
   - Error case: Send command with non-existent PageId, assert failure/exception behavior

   Tests for **API-04 FlagArticle** (POST /articles/{pageId}/flag):
   - Happy path: Seed article, send `FlagArticleCommand { PageId = 100, Reason = "Outdated content" }`, verify ArticleFlag row in DB
   - Error case: Send command with empty reason (should fail FluentValidation)

   Tests for **API-05 GetReadStats** (GET /articles/{pageId}/readstats):
   - Happy path: Seed article + mark as read, send `GetReadStatsQuery { PageId = 100 }` (or `GetDetailedReadStatsQuery`), assert reader appears in results
   - NOTE: Read the actual query class to determine the correct type name and properties

4. **Create `FavoriteEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`
   - Constructor calls cleanup

   Tests for **API-07 GetFavorites** (GET /favorites):
   - Happy path: Seed article + toggle favorite, send `GetFavoritesQuery`, assert result contains the favorited article
   - No favorites: Send query with clean DB, assert empty result

   Tests for **API-08 ToggleFavorite** (POST /favorites/{pageId}):
   - Happy path (add): Seed article, send `ToggleFavoriteCommand { PageId = 100 }`, verify Favorite row exists in DB
   - Happy path (remove): Toggle twice, verify Favorite row is removed after second toggle
   - Error case: Toggle for non-existent article

5. **Create `ApprovalEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`
   - Constructor calls cleanup

   Tests for **API-06 ApproveArticle** (POST /articles/{pageId}/approve):
   - Happy path (approve): Seed article with status "InReview", send `ApproveArticleCommand { PageId = 100, Action = "Approved", Comment = "Looks good" }`, verify status changed to "Published" in DB and ApprovalHistory row created
   - Happy path (reject): Seed InReview article, send with Action = "Rejected" + Comment, verify status changed to appropriate rejection status
   - Error case: Approve a Draft article (invalid transition), assert failure
   - NOTE: Read the ApproveArticleCommand source to determine exact property names (Action vs Status, etc.)
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && docker-compose -f docker/docker-compose.yml up -d && sleep 5 && cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development --filter "FullyQualifiedName~ArticleEndpoint|FullyQualifiedName~FavoriteEndpoint|FullyQualifiedName~ApprovalEndpoint" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - UnitTest1.cs does NOT exist
    - ArticleEndpointTests.cs contains `class ArticleEndpointTests : IntegrationTestBase`
    - ArticleEndpointTests.cs contains `MarkAsReadCommand` or `GetArticleStatusQuery`
    - ArticleEndpointTests.cs contains at least 6 `[Fact]` attributes
    - FavoriteEndpointTests.cs contains `class FavoriteEndpointTests : IntegrationTestBase`
    - FavoriteEndpointTests.cs contains `ToggleFavoriteCommand`
    - FavoriteEndpointTests.cs contains at least 3 `[Fact]` attributes
    - ApprovalEndpointTests.cs contains `class ApprovalEndpointTests : IntegrationTestBase`
    - ApprovalEndpointTests.cs contains `ApproveArticleCommand`
    - ApprovalEndpointTests.cs contains at least 3 `[Fact]` attributes
    - `dotnet test tests/WissensHub.Tests` succeeds with 0 errors
  </acceptance_criteria>
  <done>Article, Favorite, and Approval endpoint test files compile and pass with happy-path + error-case tests for API-01 through API-08</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard, ReadStats, and Admin endpoint tests + run all</name>
  <files>
    api/tests/WissensHub.Tests/Endpoints/DashboardEndpointTests.cs
    api/tests/WissensHub.Tests/Endpoints/ReadStatsEndpointTests.cs
    api/tests/WissensHub.Tests/Endpoints/AdminEndpointTests.cs
  </files>
  <read_first>
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestFixture.cs
    api/tests/WissensHub.Tests/Infrastructure/IntegrationTestBase.cs
    api/tests/WissensHub.Tests/Endpoints/ArticleEndpointTests.cs
    api/src/WissensHub.Application/Queries/GetDashboardStats/GetDashboardStatsQuery.cs
    api/src/WissensHub.Application/Queries/GetDetailedReadStats/GetDetailedReadStatsQuery.cs
    api/src/WissensHub.Application/Queries/GetAdminReports/GetAdminReportsQuery.cs
    api/src/WissensHub.Application/Queries/GetAllCategories/GetAllCategoriesQuery.cs
    api/src/WissensHub.Application/Queries/GetAllTargetGroups/GetAllTargetGroupsQuery.cs
    api/src/WissensHub.Application/Queries/GetReminderInterval/GetReminderIntervalQuery.cs
    api/src/WissensHub.Application/Commands/CreateCategory/CreateCategoryCommand.cs
    api/src/WissensHub.Application/Commands/UpdateCategory/UpdateCategoryCommand.cs
    api/src/WissensHub.Application/Commands/DeleteCategory/DeleteCategoryCommand.cs
    api/src/WissensHub.Application/Commands/CreateTargetGroup/CreateTargetGroupCommand.cs
    api/src/WissensHub.Application/Commands/UpdateReminderInterval/UpdateReminderIntervalCommand.cs
    api/src/WissensHub.Domain/Entities/Category.cs
    api/src/WissensHub.Domain/Entities/TargetGroup.cs
  </read_first>
  <action>
1. **Create `DashboardEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`
   - Constructor calls cleanup

   Tests for **API-09 GetDashboardStats** (GET /dashboard/stats):
   - Happy path: Seed 3 Published articles (2 mandatory), send `GetDashboardStatsQuery`, assert unread count = 2 (mandatory only) or total article count (read the query handler to determine stat calculation logic)
   - With favorites: Seed article + toggle favorite + query, assert favorites count = 1
   - Empty state: Query with no data, assert all counts = 0

2. **Create `ReadStatsEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`
   - Constructor calls cleanup

   Tests for **API-05 GetDetailedReadStats** (GET /articles/{pageId}/readstats):
   - Happy path: Seed article + mark as read, send detailed read stats query, assert reader info in response
   - No readers: Seed article without any reads, assert empty reader list
   - NOTE: This may use `GetDetailedReadStatsQuery` or `GetReadStatsQuery` -- read the actual source files to determine the correct query class name and its properties

3. **Create `AdminEndpointTests.cs`:**
   - Class extends `IntegrationTestBase`
   - Constructor calls cleanup

   Tests for **API-10 GetAdminReports** (GET /admin/reports):
   - Happy path: Seed 3 articles across different statuses, send `GetAdminReportsQuery`, assert report contains all articles with correct status breakdown
   - Empty: Query with no data, assert empty report

   Tests for **Categories CRUD:**
   - Create: Send `CreateCategoryCommand { Name = "Test Category", Description = "Test" }`, verify Category row in DB
   - Read: Create + send `GetAllCategoriesQuery`, assert category appears in results
   - Update: Create + send `UpdateCategoryCommand { Id = ..., Name = "Updated" }`, verify name changed in DB
   - Delete: Create + send `DeleteCategoryCommand { Id = ... }`, verify row removed from DB
   - Duplicate name: Create category, try to create another with same name, assert validation failure

   Tests for **Target Groups:**
   - Create: Send `CreateTargetGroupCommand { Name = "Test Group", SharePointGroupName = "Test SP Group" }`, verify row in DB
   - Read: Create + query, assert group appears

   Tests for **Reminder Interval:**
   - Read: Send `GetReminderIntervalQuery`, assert returns default or configured interval
   - Update: Send `UpdateReminderIntervalCommand { IntervalDays = 14 }`, query again, assert updated value

4. **Run full test suite** against Docker SQL Server:
   - Ensure Docker SQL Server is running: `docker-compose -f docker/docker-compose.yml up -d`
   - Run: `cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development --verbosity normal`
   - All tests must pass including existing DatabaseSchemaTests
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub && docker-compose -f docker/docker-compose.yml up -d && sleep 5 && cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DashboardEndpointTests.cs contains `class DashboardEndpointTests : IntegrationTestBase`
    - DashboardEndpointTests.cs contains `GetDashboardStatsQuery`
    - DashboardEndpointTests.cs contains at least 2 `[Fact]` attributes
    - ReadStatsEndpointTests.cs contains `class ReadStatsEndpointTests : IntegrationTestBase`
    - ReadStatsEndpointTests.cs contains at least 2 `[Fact]` attributes
    - AdminEndpointTests.cs contains `class AdminEndpointTests : IntegrationTestBase`
    - AdminEndpointTests.cs contains `CreateCategoryCommand`
    - AdminEndpointTests.cs contains `GetAdminReportsQuery`
    - AdminEndpointTests.cs contains at least 8 `[Fact]` attributes
    - `dotnet test tests/WissensHub.Tests` passes with 0 failures
    - Total backend test count is at least 25 (6 existing schema + 19+ new endpoint tests)
  </acceptance_criteria>
  <done>All 10 API endpoints covered by integration tests; all tests pass against real SQL Server in Docker; total backend test count exceeds 25</done>
</task>

</tasks>

<verification>
- `cd api && dotnet test tests/WissensHub.Tests -e AZURE_FUNCTIONS_ENVIRONMENT=Development` passes with 0 failures
- 6 endpoint test files exist in Endpoints/ directory
- UnitTest1.cs is deleted
- Total test count >= 25
</verification>

<success_criteria>
All 10 Azure Functions API endpoints have integration tests exercising the full MediatR pipeline against real SQL Server. Each endpoint has at least one happy-path and one error-case test. All tests pass via `dotnet test`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing/11-04-SUMMARY.md`
</output>
