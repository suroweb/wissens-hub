---
phase: 11-testing
plan: 04
type: execute
wave: 3
depends_on: [11-00]
files_modified:
  - e2e/tests/01-dashboard.spec.ts
  - e2e/tests/02-mark-as-read.spec.ts
  - e2e/tests/03-approve-article.spec.ts
  - e2e/tests/04-admin-config.spec.ts
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Dashboard flow test verifies article browsing, filtering, search, and view toggle"
    - "Mark-as-read flow test verifies sidebar read confirmation"
    - "Approve article flow test verifies Freigabecenter approval/rejection"
    - "Admin config flow test verifies category management"
    - "All E2E tests use Edge SSO auth and workbench fixtures"
  artifacts:
    - path: "e2e/tests/01-dashboard.spec.ts"
      provides: "Dashboard E2E test"
      contains: "Dashboard"
    - path: "e2e/tests/02-mark-as-read.spec.ts"
      provides: "Mark-as-read E2E test"
      contains: "mark"
    - path: "e2e/tests/03-approve-article.spec.ts"
      provides: "Approve article E2E test"
      contains: "approve"
    - path: "e2e/tests/04-admin-config.spec.ts"
      provides: "Admin config E2E test"
      contains: "admin"
  key_links:
    - from: "e2e/tests/01-dashboard.spec.ts"
      to: "e2e/fixtures/spfx-fixtures.ts"
      via: "test fixture import"
      pattern: "spfx-fixtures"
    - from: "e2e/tests/01-dashboard.spec.ts"
      to: "e2e/global-setup.ts"
      via: "Playwright global setup for auth"
      pattern: "globalSetup"
---

<objective>
Create 4 Playwright E2E test files covering the critical user flows against the SharePoint workbench: dashboard browsing, mark-as-read, article approval, and admin configuration.

Purpose: Validates end-to-end functionality through real browser interaction with the SharePoint workbench running the local dev server.
Output: 4 E2E spec files exercising all critical user flows.
</objective>

<execution_context>
@/Users/dancomilosevici/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dancomilosevici/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-testing/11-CONTEXT.md
@.planning/phases/11-testing/11-RESEARCH.md
@.planning/phases/11-testing/11-00-SUMMARY.md

<interfaces>
<!-- Workbench fixture from Plan 00 -->
From e2e/fixtures/spfx-fixtures.ts:
```typescript
export function loadWorkbench(page: Page, webPartName: string): Promise<void>;
export function resetWorkbenchState(page: Page): Promise<void>;
// createWebPartFixture returns a custom test fixture for a specific web part
export function createWebPartFixture(webPartName: string): TestType<...>;
```

<!-- Web part display names for workbench toolbox -->
Dashboard: "Dashboard"
ArticleSidebar: "ArticleSidebar"  (or "Artikel-Sidebar" -- check manifest)
Freigabecenter: "Freigabecenter"
AdminPanel: "AdminPanel"  (or "Admin-Panel" -- check manifest)

<!-- Key UI selectors (German locale) -->
Dashboard: card view, list view, filter dropdowns, search input, stats bar
ArticleSidebar: metadata section, mark-as-read button, flag button, favorite toggle
Freigabecenter: Pivot tabs (Ausstehend, Markiert, Veraltet), approve/reject buttons
AdminPanel: Pivot tabs (Kategorien, Zielgruppen, Berichte, Uebersicht), add/edit/delete

<!-- CRITICAL: tests run against workbench with mock data (no real SharePoint/API) -->
<!-- Components fall back to mock services when served from localhost -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard and Mark-as-Read E2E tests</name>
  <files>
    e2e/tests/01-dashboard.spec.ts
    e2e/tests/02-mark-as-read.spec.ts
  </files>
  <read_first>
    e2e/fixtures/spfx-fixtures.ts
    e2e/playwright.config.ts
    e2e/global-setup.ts
    ../forge-spfx-webpart-project-management-00/verification-tests/e2e/01-full-lifecycle.spec.ts
    spfx/src/webparts/dashboard/components/Dashboard.tsx
    spfx/src/webparts/articleSidebar/components/ArticleSidebar.tsx
    spfx/src/webparts/dashboard/DashboardWebPart.manifest.json
    spfx/src/webparts/articleSidebar/ArticleSidebarWebPart.manifest.json
  </read_first>
  <action>
Read the web part manifest files to get the exact display names used in the workbench toolbox (the `preconfiguredEntries[0].title.default` field).

**01-dashboard.spec.ts:**
```typescript
import { test, expect, resetWorkbenchState } from '../fixtures/spfx-fixtures';
```

- Use `test.describe.serial('Dashboard Flow', () => { ... })` for sequential execution
- `test.beforeEach`: call `resetWorkbenchState(workbenchPage)`
- The workbench fixture loads the Dashboard web part once (worker-scoped)

Tests (each wrapped in `test('...', async ({ workbenchPage }) => { ... })`):

1. **Web part renders on workbench:**
   - Wait for web part container to be visible (use `[data-automationid]` or CSS class containing "dashboard")
   - `await expect(webpart).toBeVisible({ timeout: 15_000 })`

2. **Article cards are displayed:**
   - Wait for article cards to appear (MockPageService returns 6 Published articles)
   - Assert at least one article title is visible (e.g., "Passwort-Richtlinie" from mock data)
   - `await expect(workbenchPage.getByText('Passwort-Richtlinie')).toBeVisible()`

3. **Toggle between card and list view:**
   - Find view toggle button (look for aria-label containing "Listenansicht" or "ListView")
   - Click toggle
   - Assert list view is now visible (DetailsList or table element)
   - Click toggle back to card view

4. **Search filters articles:**
   - Find search input (placeholder "Artikel suchen..." or similar)
   - Type "Datenschutz" into search
   - Wait for filtered results
   - Assert "DSGVO-Grundlagen" is visible and "Passwort-Richtlinie" is not (or filtered out)
   - Clear search input

5. **Stats bar shows counts:**
   - Assert stats bar section is visible
   - Assert unread count is displayed (number visible in stats area)

**02-mark-as-read.spec.ts:**
- This test needs the ArticleSidebar web part loaded on the workbench
- Use the fixture to load the ArticleSidebar web part name from the manifest
- NOTE: Since the workbench loads one web part per worker, this test file needs its own fixture instance. Use `createWebPartFixture('ArticleSidebar')` or load the web part by name.
- If the fixture doesn't support multiple web parts, adapt: add a second web part to the same workbench page (click "+" again) or reset and load ArticleSidebar

Tests:
1. **Sidebar renders article metadata:**
   - Wait for sidebar content to be visible
   - Assert metadata section shows article info (category, status, author from mock data)

2. **Mark-as-read button works:**
   - Find the "Als gelesen markieren" / mark-as-read button
   - Click the button
   - Wait for optimistic UI update
   - Assert read confirmation appears ("Sie haben am..." or date display)
   - NOTE: In workbench with mock services, the mock handles this via MockReadConfirmationService

3. **Favorite toggle works:**
   - Find the favorite toggle button (star icon)
   - Click to toggle
   - Assert visual state changes (filled star vs outline)

NOTE: E2E tests run against workbench which uses mock services (isServedFromLocalhost = true). This means all data is mock data from MockPageService, MockApiClient, etc. Tests verify the UI interaction, not real API calls.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/e2e && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/01-dashboard.spec.ts contains `test.describe.serial('Dashboard`
    - e2e/tests/01-dashboard.spec.ts contains `resetWorkbenchState`
    - e2e/tests/01-dashboard.spec.ts contains at least 4 `test(` blocks
    - e2e/tests/01-dashboard.spec.ts contains `toBeVisible`
    - e2e/tests/02-mark-as-read.spec.ts contains at least 2 `test(` blocks
    - e2e/tests/02-mark-as-read.spec.ts contains reference to mark-as-read or "gelesen"
    - Both files import from `../fixtures/spfx-fixtures`
    - TypeScript compilation succeeds (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Dashboard and mark-as-read E2E tests exist with serial test execution, workbench fixture usage, and meaningful assertions for rendering, filtering, search, view toggle, and read confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Approve article and Admin config E2E tests</name>
  <files>
    e2e/tests/03-approve-article.spec.ts
    e2e/tests/04-admin-config.spec.ts
  </files>
  <read_first>
    e2e/fixtures/spfx-fixtures.ts
    e2e/tests/01-dashboard.spec.ts
    spfx/src/webparts/freigabecenter/components/Freigabecenter.tsx
    spfx/src/webparts/freigabecenter/components/PendingTab.tsx
    spfx/src/webparts/freigabecenter/components/ApproveDialog.tsx
    spfx/src/webparts/adminPanel/components/AdminPanel.tsx
    spfx/src/webparts/adminPanel/components/KategorienTab.tsx
    spfx/src/webparts/freigabecenter/FreigabecenterWebPart.manifest.json
    spfx/src/webparts/adminPanel/AdminPanelWebPart.manifest.json
  </read_first>
  <action>
Read the web part manifest files for exact display names.

**03-approve-article.spec.ts:**
- Import test fixtures from `../fixtures/spfx-fixtures`
- `test.describe.serial('Approve Article Flow', () => { ... })`
- Load Freigabecenter web part on workbench

Tests:
1. **Freigabecenter renders with tabs:**
   - Wait for Freigabecenter container to be visible
   - Assert Pivot tabs are visible (look for tab text: "Ausstehend", "Markiert", "Veraltet" or similar German labels -- read the component source)

2. **Pending tab shows articles awaiting approval:**
   - Click on pending/Ausstehend tab if not default
   - Wait for article list to appear
   - Assert at least one article title is visible (mock data has InReview articles: "Datensicherung-Konzept")
   - Assert approve/reject buttons are visible for the article

3. **Approve dialog opens and submits:**
   - Click approve button on the first pending article
   - Wait for dialog to appear
   - Assert dialog is visible with optional comment field
   - Type "Inhalt geprueft" in comment field
   - Click submit/confirm button
   - Wait for dialog to close
   - Assert article is removed from pending list or status changes (mock service handles optimistically)

4. **Flagged tab shows flagged articles:**
   - Click on flagged/Markiert tab
   - Wait for flagged articles to appear
   - Assert flag reason is visible (mock data has a flagged article with reason text)

**04-admin-config.spec.ts:**
- Import test fixtures
- `test.describe.serial('Admin Config Flow', () => { ... })`
- Load AdminPanel web part on workbench

Tests:
1. **Admin panel renders with tabs:**
   - Wait for AdminPanel container to be visible
   - Assert Pivot tabs visible (Kategorien, Zielgruppen, Berichte, Uebersicht)

2. **Categories tab shows existing categories:**
   - Click Kategorien tab if not default
   - Wait for category list to render
   - Assert mock categories are visible ("IT-Sicherheit", "Datenschutz", etc. from MOCK_ADMIN_CATEGORIES)

3. **Add category dialog:**
   - Click "Hinzufuegen" / add button
   - Wait for dialog/form to appear
   - Type "Neue Kategorie" in name field
   - Type "Beschreibung" in description field
   - Click save/submit button
   - Wait for dialog to close
   - Assert new category appears in the list (mock service adds to local state)

4. **Overview tab shows article stats:**
   - Click Uebersicht tab
   - Wait for overview content to render
   - Assert article count or status breakdown is visible

NOTE: Like other E2E tests, these run against mock services in the workbench. The mock services maintain session state, so actions like approve/add-category reflect immediately in the UI within the same browser session.
  </action>
  <verify>
    <automated>cd /Users/dancomilosevici/DevLeet/Microsoft-365-Solutions/wissens-hub/e2e && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/03-approve-article.spec.ts contains `test.describe.serial('Approve`
    - e2e/tests/03-approve-article.spec.ts contains at least 3 `test(` blocks
    - e2e/tests/03-approve-article.spec.ts references Freigabecenter or approval
    - e2e/tests/04-admin-config.spec.ts contains `test.describe.serial('Admin`
    - e2e/tests/04-admin-config.spec.ts contains at least 3 `test(` blocks
    - e2e/tests/04-admin-config.spec.ts references categories or "Kategorien"
    - Both files import from `../fixtures/spfx-fixtures`
    - TypeScript compilation succeeds (`npx tsc --noEmit` exits 0)
    - All 4 E2E test files exist in e2e/tests/
  </acceptance_criteria>
  <done>Approve article and admin config E2E tests exist with serial execution, workbench fixtures, and meaningful assertions for approval workflow and category management</done>
</task>

</tasks>

<verification>
- `cd e2e && npx tsc --noEmit` passes (TypeScript compilation)
- All 4 spec files exist: 01-dashboard, 02-mark-as-read, 03-approve-article, 04-admin-config
- Each spec file has at least 2 test blocks
- All specs import from fixtures/spfx-fixtures
- NOTE: Actual E2E execution requires: (1) Docker SQL Server running, (2) SPFx dev server running via `npm start`, (3) Edge with SharePoint SSO. This is a `checkpoint:human-verify` scenario handled in Plan 05.
</verification>

<success_criteria>
4 Playwright E2E spec files cover all critical user flows: dashboard browsing/filtering/search, mark-as-read, article approval, and admin category management. Files compile cleanly and follow the proven forge-spfx-webpart-project-management-00 pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing/11-04-SUMMARY.md`
</output>
